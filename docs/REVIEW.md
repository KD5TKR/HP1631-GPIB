# HP 1630/1631A — disassembly review via the function map
Call graph + architecture derived from FUNCTIONS.md (1,037 functions, 330 adv/coop-refined) and the ROM byte scan. Generated by `re/tools/review_functions.py`.

## B0054 — 13 functions
**Hub routines (most-called within ROM):**
- `9EDF` (called 4x) — prints "ADDR DATA STAT EXTRA4v0"
- `9C2D` (called 2x) — Branch if not equal (BNE) to continue a loop that copies 80 bytes ($50
- `9F14` (called 1x) — prints "ADDR DATA STAT EXTRA4v0"
- `9F64` (called 1x) — Compare U against M3252 (likely a menu/state identifier).
- `9C97` (called 1x) — prints "compatible"
- `9CBD` (called 1x) — Call a kernel routine at Z35C7 (likely a display/CRTC function).

**Entry roots (call others, not called within ROM — dispatched/boot):**
- `90E6` -> Set interrupt mask bits (IRQ/FIRQ) for critical section.
- `9D9D` -> prints "EXTRA4P3B"
- `9DD0` -> Store A into M2786 (update a configuration or mode flag).
- `9F2D` -> prints "EXTRA4v0"

53 cross-bank calls (into kernel/other banks).

## B0055 — 126 functions
**Hub routines (most-called within ROM):**
- `9B67` (called 18x) — Point X to table M9998 (PCR-relative), likely a disc command/parameter
- `8A5D` (called 8x) — Calls subroutine Z8E18, which handles the HP-IB or disc operation for 
- `8450` (called 8x) — Call subroutine to initialize HP-IB/disc operation.
- `9006` (called 8x) — This PSHS saves registers U, X, and D at the start of a subroutine, pa
- `9038` (called 8x) — Save U, X, D registers for disc/HP-IB file system subroutine.
- `99A0` (called 8x) — Checks HP-IB status byte B for $FC or negative, sets error code in MDE ·
- `8004` (called 7x) — Jump to subroutine at 8014 (HP-IB byte counter logic).
- `8539` (called 6x) — prints "storing configuration"

**Entry roots (call others, not called within ROM — dispatched/boot):**
- `89DA` -> prints "internal: undefined error"
- `8D3A` -> Branch if table entry found to HP‑IB handler Z8C6F.
- `98BB` -> Compares directory entry count against stored value at M9695. ·
- `991B` -> Compares user input string against command table at M930E, sets error  ·
- `9AC0` -> Load byte from stack for HP-IB status/error processing.
- `9B6F` -> This LDU instruction loads U with constant #5 in the disc/HP-IB ROM (B
- `9BA2` -> This instruction ANDs accumulator A with a byte from the stack (at S),
- `9C4C` -> This rotates the byte at offset 1 from X left, part of a multi-byte sh
- `9DE5` -> Disable interrupts before BCD seconds counter update (M2A72).
- `B7CB` -> prints "None  4V"

272 cross-bank calls (into kernel/other banks).

## B0056 — 141 functions
**Hub routines (most-called within ROM):**
- `AF8C` (called 11x) — Load byte count 8 for block copy (e.g., 8 bytes of data).
- `A79B` (called 9x) — Tests M26A1 flag to branch on calibration condition. ·
- `AFBA` (called 8x) — Wait loop for analog status (target AFBF)
- `88CA` (called 7x) — Add offset to convert analog channel number to CRTC address.
- `B5CE` (called 6x) — This branch (BRA) jumps to ZB5E8, part of a switch-case structure that
- `9BB5` (called 5x) — Display analog board missing error message if M2786 is zero. ·
- `AFAA` (called 5x) — Load status from I/O register $E838 (analog/threshold ready flag).
- `8417` (called 4x) — Long call to subroutine Z847E (analog sequence handler).

**Entry roots (call others, not called within ROM — dispatched/boot):**
- `8095` -> Load pointer to analog configuration parameter table at $412E in work 
- `81DE` -> Jump to analog power failure error handler at Z821C.
- `829E` -> Long call to analog trigger handler at 842F
- `83D4` -> Branch to return with error code $FE (analog threshold error).
- `865F` -> Pull condition codes from stack (restore interrupt state).
- `86E8` -> Long call to Z3A54 (copy/format data to display buffer).
- `8C47` -> prints "#|^v%   abcd<>u xxx-4v"
- `8F08` -> Branches unconditionally to Z8F1D to set B to #$08 (instead of #$07) w
- `941B` -> Branch if M201D (acq mode) = 1 (state) to set display format 8.
- `948F` -> Point X to analog calibration parameter table at M93CA.

447 cross-bank calls (into kernel/other banks).

## B0057 — 105 functions
**Hub routines (most-called within ROM):**
- `9097` (called 8x) — LBSR ZC7F9 at B0057:909F calls a subroutine that processes a display-l
- `921B` (called 6x) — CMPA #$00 – Check menu index (M2739) for zero in BULK2 dispatch.
- `8605` (called 5x) — Sets analog threshold voltage via M2719 ORed with $10 to $E836. ·
- `9CB1` (called 5x) — This transfers U to X to set up a pointer for a subsequent subroutine 
- `9CBD` (called 5x) — This transfers U to X to set up a pointer for a subsequent subroutine 
- `9CC9` (called 5x) — This transfers U to X to set up a pointer for a subsequent subroutine 
- `818D` (called 4x) — Load X with address M001D, likely a menu template or string pointer.
- `80E0` (called 4x) — Branches if B != 0 to Z80FC, part of a state/timing configuration menu

**Entry roots (call others, not called within ROM — dispatched/boot):**
- `81D4` -> entry_81D4 — PSHS A; ... menu helper
- `8326` -> Stores zero (CLRA/CLRB) into M2683 to initialize a counter or flag use
- `83E8` -> entry_83E8 — -> LBSR $9B6E
- `848E` -> entry_848E — -> LBSR kernel $D941 (cross-bank call into resident kerne
- `9264` -> Branches if B is not equal to #$00 (BNE Z9285) as part of a multi-way 
- `953C` -> Copies stack frame to M272E and stores menu pointer to M273A. ·
- `95CA` -> Loads M90EB template, stores menu pointer, tests M23DA config active f ·
- `961E` -> Initialize state/timing config menu based on M23EE mode flag. ·
- `966B` -> Initialize second state/timing config menu based on M2400 flag. ·
- `9AA2` -> Compare string at M982D and update threshold M269B via ZC5A7. ·

305 cross-bank calls (into kernel/other banks).

## B0058 — 152 functions
**Hub routines (most-called within ROM):**
- `B5E6` (called 9x) — This calls subroutine Z5AA5 with A=0x47 ('G'), likely to output a char
- `B5D8` (called 9x) — This calls subroutine Z5AA5 with A=0x47 ('G'), likely to output a char
- `AAA0` (called 6x) — Compare acquisition mode at M201D to 2 for timing flag. ·
- `BF9E` (called 6x) — Load MC6845 register index from CRTC address port $C604
- `BF5C` (called 6x) — This stores the result of an XOR/OR accumulation back to the output bu
- `9661` (called 4x) — Call display/CRTC subroutine at ZEB82
- `89C0` (called 4x) — Tests mode byte and flag M2001 to conditionally skip display processin ·
- `9447` (called 4x) — Load byte at offset 3 of X (likely a limit or count value).

**Entry roots (call others, not called within ROM — dispatched/boot):**
- `82A1` -> Unconditional long branch (LBRA Z99D7) to a routine in the acquisition
- `83B5` -> Save U, X, A for indexed display string routine.
- `879B` -> This loads A from M2779 and compares it to #$03, checking a mode or st
- `8864` -> Computes index into parameter table M20F8 or M2086 based on mode flag. ·
- `8B5C` -> Displays string from indirect table M8AC3 indexed by M201D, saves curs ·
- `8E66` -> Load X with constant 6 for field width/format argument.
- `8F6F` -> Call subroutine at Z1864 (acquisition or display routine)
- `99E3` -> Load X with a PC-relative address (M970B), part of the acquisition/lis
- `9A58` -> LBSR ZB5D9 - This calls a subroutine in the acquisition+listing ROM (B
- `A27A` -> This LDD #M0004 loads the constant 4 into D-register as part of a swit

320 cross-bank calls (into kernel/other banks).

## B0059 — 183 functions
**Hub routines (most-called within ROM):**
- `95E8` (called 14x) — uic_95E8 — LBSR'd after uic_969F with a table ptr — render/format a UI
- `969F` (called 13x) — uic_969F — alloc/clear: PSHS U,X,D; MUL; ADDD #8; clear loop; returns 
- `880C` (called 10x) — ORs three status bytes at X+1..X+3, returns Z flag set if all zero. ·
- `B156` (called 9x) — Conditionally displays M268B value or advances cursor 9 columns for me ·
- `B75C` (called 9x) — Compare sample value to threshold 4 for bin classification.
- `8788` (called 8x) — This instruction checks if the increment of a 24-bit counter at X+2 ca
- `88D5` (called 8x) — Parses two arguments from stack, stores results in local buffer. ·
- `87DF` (called 7x) — Multi-byte right shift of 4-byte value at X+1 through X+4. ·

**Entry roots (call others, not called within ROM — dispatched/boot):**
- `8819` -> Displays error message via display_string if status flags nonzero. ·
- `8E6F` -> Branch if measurement count non-zero to skip default minimum set.
- `8E7D` -> Stores the 16-bit result from subroutine Z85A7 into stack offset $0B, 
- `8F12` -> This instruction stores a byte from A into memory pointed by U (post-i
- `8FCF` -> This STD stores a 16-bit value from the U pointer into offset 3 of a l
- `9075` -> Call subroutine Z89DF, probably a display or formatting routine.
- `91B8` -> Conditional branch based on signed comparison; decide whether to displ
- `9216` -> This STB ,S stores a byte from M2A32 (a keyboard scan code or state) o
- `9813` -> Branch if measurement state flag is zero (skip cleanup)
- `9B3B` -> Calls display attribute setter Z71B8, then increments measurement UI c ·

356 cross-bank calls (into kernel/other banks).

## B0060 — 151 functions
**Hub routines (most-called within ROM):**
- `AB82` (called 27x) — Increments HP-IB byte counter and stores 16-bit result to M326C. ·
- `8CCC` (called 22x) — Zero the HP-IB byte counter for disc write.
- `9280` (called 19x) — Calls Z922C twice to fetch and combine two measurement bytes. ·
- `8C80` (called 18x) — This instruction calls subroutine ZD5C7 via LBSR, part of the measurem
- `A7BC` (called 14x) — Accumulates 16-bit buffer values into histogram via ZADD8 until counte ·
- `9287` (called 9x) — Clears M2996 and M2995 flags for state/timing configuration. ·
- `8604` (called 8x) — Stores A (value #$01) into M207F, part of a routine in the measurement
- `8088` (called 7x) — Store accumulator to offset 4 in X (likely a structure field)

**Entry roots (call others, not called within ROM — dispatched/boot):**
- `8229` -> Load display format code $0710 for histogram rendering.
- `8782` -> LBSR to display_string routine at $D089 (banked overlay)
- `881C` -> Branch if first measurement equals threshold to skip error increment.
- `901D` -> Compare with byte at $2002 (acquisition parameter or threshold).
- `9096` -> Clamps measurement result to 1000 and stores in M2695. ·
- `938C` -> Polls Z922C in loop until result is zero. ·
- `9A89` -> Calls subroutine ZD1E6 with A=8, part of a data processing routine in 
- `9AE3` -> Compare U register to address MB7DA, likely a buffer or table limit.
- `9D84` -> Call routine to finalize display or data formatting
- `A301` -> Computes horizontal coordinates for two waveform traces using scaling  ·

396 cross-bank calls (into kernel/other banks).

## B0061 — 161 functions
**Hub routines (most-called within ROM):**
- `F637` (called 35x) — mmu_set_page — (A=logical page idx, D/X=phys) -> writes $FF00 page reg
- `CB82` (called 18x) — Call Z5FF8 (likely routine in bank $05 for analog)
- `F5C7` (called 13x) — Set X to character pattern pointer from mapping table (font).
- `C974` (called 12x) — Loads B with 4 to initialize a loop counter for a BCD-to-binary conver
- `E7BF` (called 10x) — Copies a NUL-terminated display list template from U to buffer at X. ·
- `F1E6` (called 9x) — Increment counter/index in configuration loop.
- `F230` (called 8x) — Call kernel subroutine ZFCAC (interrupt handler)
- `FAA5` (called 7x) — This BLT instruction compares the accumulator A against a value at I/O

**Entry roots (call others, not called within ROM — dispatched/boot):**
- `C8E8` -> This BLE instruction checks if A ≤ 1, controlling a loop that processe
- `CAB7` -> Self-test error code switch comparing B to 3. ·
- `CACA` -> Branch to common exit after case 3 second message display. ·
- `CAE3` -> Call display routine to print first case 4/6 message.
- `CAEC` -> Call display routine to print first case 4/6 message.
- `CCBA` -> This computes the address of a stack-based buffer (offset $05 from S) 
- `CFAD` -> Stores display list pointer and attribute into menu structure fields. ·
- `D3DB` -> Outputs characters to CRTC until cursor reaches limit M28E3. ·
- `E247` -> Loads key byte from buffer, compares pointer to end M19A9, writes to $ ·
- `E885` -> Converts two stack parameters to decimal and stores at M1FBA/M1FBC. ·

212 cross-bank calls (into kernel/other banks).

## Consistency flags
0 functions whose refined/annotated purpose indicates DATA (candidates to reclassify):
