# HP 1630/1631A firmware — function map

Condensed `ROM:addr -> purpose` for routines whose purpose is known (curated label, the string it prints, or its entry annotation). Addresses are LOGICAL (B0061 maps at $C000/$F000, overlays at $8000). `(xN)` = call sites. No opcodes. Generated by `re/tools/consolidate_functions.py`.

## B0054 — PROMPT / ERROR / STATUS MESSAGES  (14 functions)

- `B0054:90E6` (x1) — Set interrupt mask bits (IRQ/FIRQ) for critical section.
- `B0054:9C2D` (x2) — Branch if not equal (BNE) to continue a loop that copies 80 bytes ($50) from source X to destination U+$12, likely copying a display buffer or configuration block.
- `B0054:9C97` (x1) — prints "compatible"
- `B0054:9CBD` (x1) — Call a kernel routine at Z35C7 (likely a display/CRTC function).
- `B0054:9D89` (x1) — prints "DATA STAT EXTRA4P3B"
- `B0054:9D93` (x1) — prints "STAT EXTRA4P3B"
- `B0054:9D9D` (x1) — prints "EXTRA4P3B"
- `B0054:9DC7` (x1) — prints "EXTRA4P3B"
- `B0054:9DD0` (x1) — Store A into M2786 (update a configuration or mode flag).
- `B0054:9EDF` (x4) — prints "ADDR DATA STAT EXTRA4v0"
- `B0054:9F14` (x1) — prints "ADDR DATA STAT EXTRA4v0"
- `B0054:9F2D` (x1) — prints "EXTRA4v0"
- `B0054:9F64` (x1) — Compare U against M3252 (likely a menu/state identifier).

## B0055 — DISC / HP-IB (LIF storage) + config menu  (126 functions)

- `B0055:8004` (x7) — Jump to subroutine at 8014 (HP-IB byte counter logic).
- `B0055:8014` (x2) — entry_8014
- `B0055:8059` (x3) — Load base address of disc/HP-IB configuration table at M940E.
- `B0055:80BD` (x1) — Save registers for disc/HP-IB address comparison routine.
- `B0055:80E0` (x4) — entry_80E0 — table dispatch on $DFB0 (LDX #$940E; LEAX D,X)
- `B0055:811B` (x2) — Call subroutine at $9BDF (HP-IB command/status handler).
- `B0055:8122` (x1) — Call subroutine at $9BDF (HP-IB command/status handler).
- `B0055:8134` (x1) — prints "None"
- `B0055:814F` (x1) — entry_814F — (refs "None ")
- `B0055:81AD` (x1) — entry_81AD
- `B0055:843D` (x2) — This LBSR calls subroutine Z1637 to perform an HP-IB or disc I/O operation (e.g., sending a command or data byte), using the value in A as a parameter and X as a pointer to a buffer, in the disc/HP-IB ROM (B0055).
- `B0055:8450` (x9) — Call subroutine to initialize HP-IB/disc operation.
- `B0055:84C5` (x1) — This compares a loaded word against a checksum or signature stored in the ROM table at 84BF (PCR-relative), verifying the integrity of a configuration block during the HP-IB/disc initialization sequence.
- `B0055:8539` (x6) — prints "storing configuration"
- `B0055:855B` (x2) — disc_store_config — "storing configuration"
- `B0055:8579` (x2) — disc_store_state — "storing state data"
- `B0055:8597` (x2) — disc_store_timing — "storing timing data"
- `B0055:85B5` (x2) — disc_store_analog — "storing analog data"
- `B0055:8601` (x4) — This instruction performs an indirect jump (JSR) to the address stored in X, which was loaded from the stack, implementing a callback or dispatch to a subroutine in the disc/HP-IB ROM (B0055), likely for executing a menu action or command.
- `B0055:862F` (x1) — Load base address MDFB4 into X, part of a disc/HP-IB (B0055) routine that checks a stored pointer or data structure for validity (comparing against MFFFE).
- `B0055:864E` (x2) — prints "loading configuration"
- `B0055:8682` (x2) — disc_load_config — "loading configuration"
- `B0055:8692` (x2) — disc_load_state — "loading state data"
- `B0055:86A2` (x2) — disc_load_timing — "loading timing data"
- `B0055:86B2` (x2) — disc_load_analog — "loading analog data"
- `B0055:86C3` (x2) — disc_load_inverse_asm — "loading inverse assembler"
- `B0055:86D3` (x2) — disc_load_executable — "loading executable"
- `B0055:8716` (x1) — Calls subroutine Z8A5D with X pointing to M18A9 and U set to #$0100 (a buffer size), part of the disc/HP-IB overlay (B0055) that likely performs a data transfer or initialization routine for LIF storage.
- `B0055:8748` (x5) — Long branch to Z82D6 (disc/HP-IB routine).
- `B0055:88F7` (x1) — disc_error_message — error dispatcher — references the whole error catalog:
- `B0055:8930` (x1) — prints "disc failure"
- `B0055:893A` (x1) — prints "unexpected EOI"
- `B0055:8944` (x1) — prints "in HPIB transfer"
- `B0055:894E` (x1) — prints "conflicting HPIB addresses"
- `B0055:8958` (x1) — prints "file not found"
- `B0055:8962` (x1) — prints "no room on disc"
- `B0055:896C` (x1) — prints "internal: disc error"
- `B0055:8976` (x1) — prints "internal: bad file type"
- `B0055:8980` (x1) — prints "internal: bad file length"
- `B0055:898A` (x1) — prints "LIF directory too big"
- `B0055:8994` (x1) — prints "disc changed"
- `B0055:899E` (x1) — prints "disc unformatted"
- `B0055:89A8` (x1) — prints "format failed"
- `B0055:89B2` (x1) — prints "directory full"
- `B0055:89BC` (x1) — prints "operation time out"
- `B0055:89C6` (x1) — prints "disc unformatted"
- `B0055:89D0` (x1) — prints "disc is write protected"
- `B0055:89DA` (x1) — prints "internal: undefined error"
- `B0055:8A2C` (x1) — prints "None"
- `B0055:8A5D` (x8) — Calls subroutine Z8E18, which handles the HP-IB or disc operation for mode 1 (e.g., reading a directory or file header) in the storage configuration menu.
- `B0055:8A7B` (x5) — prints "formatting disc"
- `B0055:8A98` (x1) — disc_format — "formatting disc" / "creating directory" (9 calls)
- `B0055:8AE0` (x1) — Load X with the address of a buffer at M930E, then clear 256 bytes (loop with B decrementing); this is a memory-clearing routine in the disc/HP-IB ROM (B0055) that initializes a data buffer for LIF storage operations.
- `B0055:8B80` (x1) — Calls subroutine Z9BDF via LBSR, which processes or renders a menu item, using the pointer in X (copied to Y) for a display list or menu template in the disc/HP-IB ROM (B0055).
- `B0055:8C15` (x1) — Store zeroed counter to RAM variable M9559 (disc transfer length).
- `B0055:8C6F` (x4) — This LBSR calls a subroutine at Z9AA9 to initialize or configure the HP-IB/disc interface buffer, likely setting up a data transfer or command for the LIF storage system.
- `B0055:8CB2` (x2) — Call subroutine to read disc/HP-IB status byte.
- `B0055:8D3A` (x1) — Branch if table entry found to HP‑IB handler Z8C6F.
- `B0055:8D98` (x1) — This STA Z9511 stores a byte from a lookup table (indexed by B) to address $9511, part of a routine in the disc/HP-IB ROM (B0055) that configures parameters (e.g., for disc or HP-IB operations) by copying values from tables at M8BB3 and M8BBA.
- `B0055:8DA1` (x1) — This STA Z9511 stores a byte from a lookup table (indexed by B) to address $9511, part of a routine in the disc/HP-IB ROM (B0055) that configures parameters (e.g., for disc or HP-IB operations) by copying values from tables at M8BB3 and M8BBA.
- `B0055:8DAA` (x1) — Store second format parameter to disc config at $9525
- `B0055:8DB5` (x1) — Store second format parameter to disc config at $9525
- `B0055:8DC6` (x1) — Copy byte from handler parameter table to kernel work area
- `B0055:8DDB` (x4) — Call disc/HP-IB command parser at Z8C15
- `B0055:8E18` (x1) — Store primary address into HP-IB config table offset 1.
- `B0055:8E5A` (x1) — This is a long branch to subroutine Z8DDB, which likely handles a disc or HP-IB operation (e.g., reading/writing a LIF file or directory entry) based on the context of loading pointers from M9516/M9518, part of the storage/config menu ROM (B0055).
- `B0055:8EB1` (x2) — Calls Z9AA9 to write a formatted message to the display (with length $68 and Y=5), part of the disc/HP-IB status message output.
- `B0055:8F07` (x2) — This loads A from stack offset 2 (the third parameter byte) and stores it into a command buffer at offset 4, part of building a parameter block for an HP-IB or disc I/O operation.
- `B0055:8F54` (x1) — Return from subroutine (PULS PC,A), concluding a routine in the disc/HP-IB overlay (B0055) that checks a status flag (MDE13) and either exits or proceeds with a disc operation (e.g., reading a directory).
- `B0055:8F96` (x1) — Load 0x0BB8 (3000 decimal) as delay count for timing loop
- `B0055:8FD9` (x1) — Load pointer to flag table at 9512 (configuration flags)
- `B0055:9006` (x8) — This PSHS saves registers U, X, and D at the start of a subroutine, part of the disc/HP-IB file system or configuration menu handler (B0055).
- `B0055:9016` (x6) — Long call to 9B67, probably a disc/HP-IB file system routine.
- `B0055:9027` (x1) — Long call to 9B67, probably a disc/HP-IB file system routine.
- `B0055:9038` (x8) — Save U, X, D registers for disc/HP-IB file system subroutine.
- `B0055:904A` (x1) — Save U, X, D registers for disc/HP-IB file system subroutine.
- `B0055:9123` (x1) — This stores a computed value (MDE0E + $40) into the second byte of a display list template at X, setting up a CRT cursor position or attribute in the disc/HP-IB ROM (B0055).
- `B0055:9188` (x1) — Adds $20 to horizontal cursor (MDE0D) for display list entry.
- `B0055:91EE` (x2) — Load pointer to message buffer at M9576 (display string area)
- `B0055:923C` (x3) — Store second converted byte to display buffer, increment pointer
- `B0055:92C8` (x1) — Initializes a stack-based pointer to zero (STX ,S), part of a loop that traverses a linked list of disc directory entries in the HP-IB/LIF storage routines (ROM B0055).
- `B0055:92D5` (x3) — Initializes a stack-based pointer to zero (STX ,S), part of a loop that traverses a linked list of disc directory entries in the HP-IB/LIF storage routines (ROM B0055).
- `B0055:9329` (x1) — This LDD #M2A06 in the disc/HP-IB overlay (B0055) loads a 16-bit value (0x2A06) used as a fill pattern for clearing a buffer at MDFD7, part of initializing a LIF storage or HP-IB communication structure.
- `B0055:93BC` (x2) — This instruction loads a pair of parameters from M9592 (likely LIF volume or directory offsets) and stores them to MDFA4, copying disc/HP-IB storage parameters into the kernel's working area for the storage routines in ROM B0055.
- `B0055:93DB` (x1) — LBSR at B0055:93EE calls subroutine Z9C17, which processes a disk/HP-IB configuration menu operation (likely comparing or copying data structures for LIF storage) in the disc/HP-IB ROM.
- `B0055:943B` (x1) — This instruction tests whether a 16-bit value at a table entry is zero, part of a routine in the disc/HP-IB ROM (B0055) that checks for empty or invalid entries in a configuration or directory table.
- `B0055:94A9` (x1) — This LDU #M900E loads a pointer to a data structure (likely a menu template or configuration table) in the prompt/error/status messages ROM (B0055), used for setting up a display list.
- `B0055:94DE` (x2) — Calls Z98E4 (a subroutine in the prompt/error/status messages overlay B0055) as part of a routine that copies 10 bytes from the stack pointer to a buffer at MDF9A, likely handling a file or device name string for HP-IB or LIF storage operations.
- `B0055:9513` (x1) — Calls Z92D5 (a disc/HP-IB utility) to perform a file system operation (likely reading a directory entry or file header), part of the LIF storage overlay (B0055) for config menu file handling.
- `B0055:952B` (x2) — This STA stores accumulator A (a byte from the source buffer) to the destination buffer at U+, part of a 32-byte block copy routine that transfers data to the HP-IB/disc interface buffer at $DFB4 in the disc/HP-IB ROM (B0055).
- `B0055:9581` (x1) — Load U with address M957D (source table for HP-IB/disc)
- `B0055:965A` (x1) — Call subroutine at Z952B (HP-IB or disc read/write operation).
- `B0055:9670` (x2) — Store byte to destination buffer for display/processing.
- `B0055:96BC` (x1) — This instruction stores D (value $0600) to memory pointed by U and increments U, initializing a data structure (likely a buffer or descriptor) for a disc/HP-IB operation (ROM B0055), setting a base address or length parameter.
- `B0055:96F3` (x3) — BNE loops in counter/decrement routine (X as counter).
- `B0055:9738` (x3) — Stores the adjusted BCD tens digit (after checking if >9 and adding $10) to the display buffer at MDFC9, part of a 7-segment display code conversion routine for the front-panel display.
- `B0055:9797` (x2) — Validates disc/HP-IB device pointer and length, sets error code if invalid.  ·refined
- `B0055:983B` (x1) — Initializes a 32-byte buffer with spaces and a 0xFF terminator.  ·refined
- `B0055:9856` (x2) — Loads a byte from $DFDF, computes offset into MDE4B via multiply by 5.  ·refined
- `B0055:98B4` (x1) — Copies a directory entry filename (31 bytes) or calls Z983B to skip it.  ·refined
- `B0055:98BB` (x1) — Compares directory entry count against stored value at M9695.  ·refined
- `B0055:991B` (x1) — Compares user input string against command table at M930E, sets error on mismatch.  ·refined
- `B0055:9960` (x1) — Copies 10 bytes from MDE16 to MDF9A if disc mode is 3 and no error.  ·refined
- `B0055:99A0` (x8) — Checks HP-IB status byte B for $FC or negative, sets error code in MDE13.  ·refined
- `B0055:99B9` (x2) — Sends HP-IB primary command (A masked to 5 bits) via Z603C, checks status.  ·refined
- `B0055:99C3` (x6) — Sends HP-IB secondary command (A masked + $20) via Z603C, checks status.  ·refined
- `B0055:99CD` (x6) — Calls Z99A0 after converting a value to a display character (adding $60 for lowercase), used in the disc/HP-IB filename or directory listing routine.
- `B0055:99D7` (x4) — Calls Z99A0 after converting a value to a display character (adding $60 for lowercase), used in the disc/HP-IB filename or directory listing routine.
- `B0055:99E1` (x6) — BSR to routine that converts and outputs a display character.
- `B0055:99E9` (x2) — LBSR to kernel routine that sends byte count to HP-IB.
- `B0055:99F1` (x1) — LBSR to kernel routine that sends byte count to HP-IB.
- `B0055:9A09` (x1) — LBSR to kernel routine that sends byte count to HP-IB.
- `B0055:9A21` (x1) — Return from subroutine in HP-IB display output routine.
- `B0055:9A28` (x5) — Return from subroutine in HP-IB display output routine.
- `B0055:9AC0` (x1) — Load byte from stack for HP-IB status/error processing.
- `B0055:9B0F` (x5) — Load mask value for HP-IB or disc status check.
- `B0055:9B67` (x18) — Point X to table M9998 (PCR-relative), likely a disc command/parameter table.
- `B0055:9B6F` (x1) — This LDU instruction loads U with constant #5 in the disc/HP-IB ROM (B0055) as a parameter for a subroutine call (Z1230) that performs a loop or countdown operation, likely for retry or timeout in disc I/O.
- `B0055:9BA2` (x1) — This instruction ANDs accumulator A with a byte from the stack (at S), testing a bit flag to decide whether to loop back and decrement a counter, part of a routine in the disc/HP-IB ROM (B0055) that processes data blocks (likely for LIF storage).
- `B0055:9BFB` (x2) — This instruction stores the result of a 32-bit subtraction (X minus U) into Y, implementing a multi-byte arithmetic operation (likely a 32-bit subtraction) used in disc/HP-IB or config menu calculations (ROM B0055).
- `B0055:9C17` (x2) — Compare with first 16-bit word from destination buffer U
- `B0055:9C40` (x2) — This rotates the byte at offset 1 from X left, part of a multi-byte shift or arithmetic routine (e.g., for 32-bit operations) in the disc/HP-IB ROM (B0055).
- `B0055:9C4C` (x2) — This rotates the byte at offset 1 from X left, part of a multi-byte shift or arithmetic routine (e.g., for 32-bit operations) in the disc/HP-IB ROM (B0055).
- `B0055:9D4E` (x2) — Initialize loop counter to 1 for bit-shifting division routine.
- `B0055:9DE5` (x1) — Disable interrupts before BCD seconds counter update (M2A72).
- `B0055:B7CB` (x1) — prints "None  4V"

## B0056 — ANALOG board self-test/UI  (141 functions)

- `B0056:8095` (x1) — Load pointer to analog configuration parameter table at $412E in work RAM.
- `B0056:8103` (x2) — This reads I/O port ME034 (LDA ME034) to get the current CRTC control register value, then sets bit 2 (ORA #$04) to enable or disable a display control signal in the analog board ROM (B0056).
- `B0056:813E` (x2) — Branch if analog threshold ready to proceed with command.
- `B0056:8183` (x1) — This STY instruction stores Y to memory location M2FDF in the analog board ROM (B0056) to save a register value as part of saving context or parameters for threshold/acquisition setup.
- `B0056:81DE` (x1) — Jump to analog power failure error handler at Z821C.
- `B0056:8273` (x2) — Return from analog interrupt status check routine (started at $8273).
- `B0056:827C` (x2) — Branch to skip analog interrupt servicing if bit 4 of $2FC6 clear.
- `B0056:829E` (x1) — Long call to analog trigger handler at 842F
- `B0056:82A7` (x2) — Long call to analog trigger handler at 842F
- `B0056:82C2` (x2) — Tests memory location M2FC9 (2FC9) to determine if a flag is set; if zero, loads $FF into B and branches to Z82EC, otherwise loads $8F and writes to I/O port ME8A3 — part of analog threshold control logic in B0056.
- `B0056:82CA` (x2) — Tests memory location M2FC9 (2FC9) to determine if a flag is set; if zero, loads $FF into B and branches to Z82EC, otherwise loads $8F and writes to I/O port ME8A3 — part of analog threshold control logic in B0056.
- `B0056:82ED` (x1) — Branch if not equal (tests A), part of a routine that selects a value (0x10 or 0x90) to store to I/O register ME8A3 (likely a control register for the keyboard/display or analog subsystem), based on a flag (M2FC9) and accumulator A, in the analog board ROM (B0056).
- `B0056:8334` (x1) — This instruction calls a subroutine (Z8302) that likely configures or initializes the analog/threshold subsystem (ROM B0056 analog board), passing A=1 as a parameter to set a specific threshold or channel mode.
- `B0056:8390` (x1) — Writes $8E to I/O port $E8A3 (part of the analog/threshold subsystem) to set a DAC or threshold voltage, then delays and reads $E8AE, configuring the analog board's comparator reference.
- `B0056:83D4` (x1) — Branch to return with error code $FE (analog threshold error).
- `B0056:8417` (x7) — Long call to subroutine Z847E (analog sequence handler).
- `B0056:847E` (x2) — BEQ Z84A7 - Branch if analog trigger status bit0 clear.
- `B0056:8517` (x1) — Branch if not mode 1; skip single-channel threshold check.
- `B0056:859B` (x1) — Store parameter into display list entry at offset $08 (field update).
- `B0056:85F8` (x1) — Load byte from RAM variable M29D7 (analog channel configuration).
- `B0056:8605` (x2) — Load immediate $14 (decimal 20, likely HP-IB timeout or delay count).
- `B0056:860F` (x1) — Load immediate $14 (decimal 20, likely HP-IB timeout or delay count).
- `B0056:865F` (x1) — Pull condition codes from stack (restore interrupt state).
- `B0056:86E8` (x1) — Long call to Z3A54 (copy/format data to display buffer).
- `B0056:88CA` (x7) — Add offset to convert analog channel number to CRTC address.
- `B0056:8901` (x4) — Add $40 to convert channel to CRTC display row address.
- `B0056:8980` (x3) — Calls subroutine Z88CA with accumulator A loaded from MDFEC, part of the analog board overlay (B0056) routine that sets threshold voltages for multiple channels.
- `B0056:89A3` (x1) — Load count of 576 ($0240) for CRTC register initialization loop.
- `B0056:89B5` (x2) — Load count of 576 ($0240) for CRTC register initialization loop.
- `B0056:8BC6` (x4) — Checks if a byte at X is zero (end of string/buffer) after decrementing counter A, part of a routine that scans for a specific character (space, $20) in a buffer, likely for parsing threshold or analog parameters.
- `B0056:8C14` (x3) — prints "#|^v%   abcd<>u xxx-4v"
- `B0056:8C47` (x1) — prints "#|^v%   abcd<>u xxx-4v"
- `B0056:8EA0` (x1) — This compares X against zero to check if a pointer (loaded from M0DA4) is null, part of a linked-list traversal in the analog board ROM (B0056), likely for threshold or channel configuration.
- `B0056:8ECB` (x1) — BNE Z8F02: Branch if pod mode not set for analog threshold config.
- `B0056:8F08` (x1) — Branches unconditionally to Z8F1D to set B to #$08 (instead of #$07) when the pod mode (M201D) is 1 or 4, selecting the number of channels for the analog board's threshold configuration.
- `B0056:8FFB` (x1) — Load word from M2782 (likely a pointer or counter variable).
- `B0056:9141` (x1) — prints "Label>"
- `B0056:9232` (x1) — prints "Time"
- `B0056:926A` (x1) — prints "Mark"
- `B0056:9293` (x1) — This LBSR calls a kernel routine (Z3A54) to perform a comparison or conversion operation, likely related to analog threshold or timing calibration in the analog board ROM B0056.
- `B0056:92CA` (x4) — Compare to $04 (select key) in keyboard handler.
- `B0056:941B` (x1) — Branch if M201D (acq mode) = 1 (state) to set display format 8.
- `B0056:9446` (x1) — prints "Marked"
- `B0056:9474` (x1) — This stores the value 0x17 (23 decimal) into the byte at offset 1 of the stack frame in the analog board ROM (B0056), setting a loop counter or parameter for a subsequent subroutine call that configures the analog threshold subsystem.
- `B0056:9483` (x1) — This loads immediate value #$45 ('E') into A to store as a display-list control byte at offset $08 of a template structure, part of building a display list for the analog board configuration menu in B0056.
- `B0056:948F` (x1) — Point X to analog calibration parameter table at M93CA.
- `B0056:94A6` (x1) — Store threshold value at offset $08 in calibration table.
- `B0056:94BD` (x1) — Load X from M277E (analog channel configuration pointer).
- `B0056:9517` (x1) — LDU     #M5B91                   *951F: Load U with address of analog config table.
- `B0056:956C` (x2) — LDX #M280E — Load pointer to analog threshold table at $280E.
- `B0056:97B3` (x2) — Initialize analog measurement loop with buffer and parameter table pointers.  ·refined
- `B0056:9806` (x1) — Set channel message pointer based on mode and analog board presence.  ·refined
- `B0056:9968` (x1) — Conditionally increment measurement count based on calibration flag and A=1.  ·refined
- `B0056:9A2B` (x2) — Clear flag M277B unless operating mode is timing analyzer (3).  ·refined
- `B0056:9AD4` (x1) — Long call to analog board initialization routine Z35DE.  ·refined
- `B0056:9AE1` (x1) — Output analog calibration table M278E bytes via Z3AA5.  ·refined
- `B0056:9B05` (x1) — Traverse linked list from reset vector $0001 for analog data structures.  ·refined
- `B0056:9B2E` (x2) — Dispatch analog command codes 0x0A, 0x0B, or 0x08 for threshold setup.  ·refined
- `B0056:9B65` (x1) — Test flag M278C for conditional analog threshold or calibration logic.  ·refined
- `B0056:9BB5` (x5) — Display analog board missing error message if M2786 is zero.  ·refined
- `B0056:9BDE` (x1) — prints "Unknown Disassembler"
- `B0056:9DB4` (x1) — Store X (returned pointer) to stack offset 2 (save buffer address).
- `B0056:9E8B` (x2) — Load pointer to analog channel configuration table M23DA.
- `B0056:A0DA` (x1) — This CMPA #$01 compares a value at M2578 to 1, used in the analog board overlay (ROM B0056) to select between two sets of threshold or DAC registers (ME83C/ME83E vs ME83C/ME83F).
- `B0056:A0E3` (x2) — This CMPA #$01 compares a value at M2578 to 1, used in the analog board overlay (ROM B0056) to select between two sets of threshold or DAC registers (ME83C/ME83E vs ME83C/ME83F).
- `B0056:A194` (x1) — Allocate 5-byte stack frame in analog calibration select routine
- `B0056:A24D` (x2) — This calls subroutine ZE9C4, likely a math or conversion routine (e.g., BCD to binary or scaling), as part of a function that processes analog threshold or calibration data in the analog board ROM.
- `B0056:A273` (x1) — Complementing B (COMB) to invert a threshold or calibration value before adding #$10 and storing to M2711, part of analog subsystem configuration in the analog board ROM (B0056).
- `B0056:A291` (x1) — This LDY loads the address of calibration table M2556 into Y, preparing to call subroutine Z28E8 for processing analog board calibration data (thresholds or gains) in ROM B0056.
- `B0056:A2A6` (x1) — Return from subroutine (end of calibration table processing).
- `B0056:A2BB` (x2) — Load U with pointer to string table MA008 (PCR-relative)
- `B0056:A2CF` (x1) — Load U with pointer to string table MA008 (PCR-relative)
- `B0056:A2E9` (x2) — Calls subroutine ZEA72 to display calibration status message.
- `B0056:A2FD` (x1) — This instruction loads the U register with address $25C4 (a data structure) during a calibration routine in the analog board ROM (B0056), preparing parameters for a threshold/voltage setup subroutine.
- `B0056:A317` (x4) — Load U with pointer to constant table MA00C (threshold values).
- `B0056:A321` (x1) — This is the entry point of a subroutine that saves registers (PSHS U,X,D) and allocates stack space, likely part of the analog board calibration or measurement routine.
- `B0056:A33B` (x3) — This LEAS -$0A,S allocates 10 bytes of stack space for local variables, part of the analog board (B0056) calibration or measurement subroutine that processes analog threshold data.
- `B0056:A36A` (x1) — This LEAS instruction adjusts the stack pointer to clean up local variables after calling ZE7F9 and ZEA72, which are part of the analog board calibration or threshold setup routine.
- `B0056:A384` (x2) — This instruction masks A with 0x3F to combine with a value from the stack, part of a routine in the analog board ROM (B0056) that sets up threshold or configuration registers (M271A) for the analog subsystem.
- `B0056:A3A8` (x2) — This instruction ORs the accumulator A with the value on the stack (which was computed from a table lookup and bit-shifted), then stores the result to $2718, which is part of the analog/threshold subsystem control registers — this configures threshold voltage levels for the analog board (ROM B0056).
- `B0056:A3CF` (x2) — Call subroutine ZA33B to compute analog parameter from M2593/M25AB.
- `B0056:A3ED` (x2) — Loads U with #M25AF to point to a calibration parameter table for the analog board, used in a subroutine that computes or stores threshold voltage settings.
- `B0056:A40B` (x1) — Store A byte to analog threshold register $E83C (ME83C).
- `B0056:A426` (x1) — Loads U with the address of M258B, setting up a source pointer for a 4-byte copy loop (to destination at M2593), part of a data initialization or configuration routine for the analog board (B0056).
- `B0056:A437` (x2) — Load U with address of source table M258F for second copy.
- `B0056:A676` (x1) — This adds the value in B to X (LEAX B,X), computing an indexed address into a table for processing threshold or calibration data in the analog board overlay (ROM B0056).
- `B0056:A680` (x1) — This adds the value in B to X (LEAX B,X), computing an indexed address into a table for processing threshold or calibration data in the analog board overlay (ROM B0056).
- `B0056:A688` (x1) — This adds the value in B to X (LEAX B,X), computing an indexed address into a table for processing threshold or calibration data in the analog board overlay (ROM B0056).
- `B0056:A6CF` (x2) — This loads the byte at M2721 into B, reading a hardware threshold or analog parameter for the analog board subsystem (ROM B0056).
- `B0056:A6FF` (x1) — This is a conditional branch that selects error code $31 or success code $22 based on a previous comparison result, part of the analog board self-test or calibration routine.
- `B0056:A755` (x1) — Disables interrupts and clears CRTC bit 4 for display init.  ·refined
- `B0056:A764` (x1) — Allocates 26-byte stack frame for analog subroutine.  ·refined
- `B0056:A79B` (x10) — Tests M26A1 flag to branch on calibration condition.  ·refined
- `B0056:A864` (x1) — Tests M26A1 flag to skip acquisition setup if set.  ·refined
- `B0056:A89C` (x1) — Tests M26A1 flag to branch on calibration path.  ·refined
- `B0056:A8F4` (x1) — Tests M26A1 flag to branch to exit after analog status.  ·refined
- `B0056:A948` (x1) — Adds M2714 to D and stores result as running sum.  ·refined
- `B0056:A96E` (x1) — Loads U with MA030 pointer for analog parameter processing.  ·refined
- `B0056:A975` (x1) — Loads U with M258B address for analog threshold processing.  ·refined
- `B0056:A9FE` (x1) — Loads X with M25A3 pointer for threshold configuration.  ·refined
- `B0056:AB02` (x1) — LBSR ZE8D5 - Call routine to update CRT display with waveform data.
- `B0056:AB54` (x1) — Call subroutine ZA48A (likely analog calibration retry routine).
- `B0056:ABAA` (x1) — Adjusts the stack pointer by adding 0x1A (LEAS $1A,S), cleaning up the stack frame after a subroutine call that performed display formatting or arithmetic (using ZEA72 and ZE5A7) in the analog board ROM (B0056).
- `B0056:ACFA` (x1) — prints "abs  4R2{"
- `B0056:AD0E` (x1) — prints "Analog Board not Present"
- `B0056:AD34` (x1) — Clears register B (sets to 0) to initialize a loop counter, part of an analog board routine (ROM B0056) that zeros out three bytes on the stack (at U+0 through U+2) before configuring the threshold subsystem via I/O at $E836.
- `B0056:AE2F` (x1) — This instruction sets U to point to the stack (S), initializing a local buffer for a subroutine that writes to I/O register $E812 (ME812), likely configuring the acquisition/timing engine (B0056 analog board) for a self-test or setup.
- `B0056:AE44` (x1) — This instruction sets U to point to the stack (S), initializing a local buffer for a subroutine that writes to I/O register $E812 (ME812), likely configuring the acquisition/timing engine (B0056 analog board) for a self-test or setup.
- `B0056:AE6D` (x4) — This redundant STA to I/O register ME812 (0xE812) is part of the analog board overlay (B0056) initializing the threshold/comparator subsystem, writing zero to clear a DAC or control register before setting it to 0x80 and 0xFC for threshold voltage configuration.
- `B0056:AED1` (x1) — This loads immediate value $07 and calls a delay/synchronization subroutine ZAF8C — part of the analog threshold calibration routine that waits for the threshold DAC to settle after writing a new value (ROM B0056).
- `B0056:AF51` (x1) — prints "Analog Board ok"
- `B0056:AF8C` (x11) — Load byte count 8 for block copy (e.g., 8 bytes of data).
- `B0056:AFAA` (x5) — Load status from I/O register $E838 (analog/threshold ready flag).
- `B0056:AFBA` (x8) — Wait loop for analog status (target AFBF)
- `B0056:B0DF` (x2) — call kernel routine ZF5E8 (keyboard input via 8279)
- `B0056:B1B8` (x1) — Store threshold table pointer into structure at offset 9.
- `B0056:B2C0` (x1) — prints "Only Available for Labels of 1 to"
- `B0056:B2F8` (x1) — call display routine Z5A54
- `B0056:B3B3` (x1) — Compute address by adding 5 to X (LEAX $05,X), part of a loop in the analog board overlay (B0056) that iterates through a data structure (e.g., threshold table or calibration points) during a configuration or calibration routine.
- `B0056:B41E` (x3) — prints "abs  4R2{"
- `B0056:B490` (x1) — Load X from stack parameter (pointer to analog channel descriptor).
- `B0056:B4BD` (x1) — This instruction loads X from the stack (LDX $02,S) to retrieve a pointer, likely used in the analog board ROM (B0056) for accessing threshold or calibration data structures.
- `B0056:B4DB` (x1) — Store calibration table pointer into channel structure offset 9.
- `B0056:B5CE` (x6) — This branch (BRA) jumps to ZB5E8, part of a switch-case structure that selects a specific byte value (0x8D) to load into A, which is then passed to a subroutine (Z5AA5) that configures the analog board's threshold DAC or comparator.
- `B0056:B661` (x1) — Branch to numeric digit handler if not '!'.
- `B0056:B70B` (x1) — prints "Pod"
- `B0056:B7CF` (x1) — prints "error:"
- `B0056:B7F2` (x1) — prints "error:"
- `B0056:BAAF` (x1) — This LDB loads the byte from memory location M202F (a hardware register or configuration byte) to set a threshold or timing parameter in the analog board ROM (B0056), storing it into a control structure at U.
- `B0056:BB35` (x1) — This STA stores accumulator A (the high byte of a shifted 16-bit value) onto the stack, part of a division or scaling routine that splits a 16-bit value into bytes for analog-to-digital conversion or threshold calculation in ROM B0056.
- `B0056:BBA3` (x1) — Stores display list entry pointer into its own offset 9 field.  ·refined
- `B0056:BBBB` (x1) — Merges two display list templates into output buffer M2049.  ·refined
- `B0056:BC95` (x1) — Stores threshold value pointer into display control block offset 9.  ·refined
- `B0056:BDE7` (x1) — Loads threshold byte from Y pointer in analog calibration loop.  ·refined
- `B0056:BE21` (x1) — Loads calibration byte from Y pointer for threshold conversion.  ·refined
- `B0056:BE40` (x1) — Calls display_string to print error message MBD93.  ·refined
- `B0056:BE4D` (x1) — Branches to common display-and-return path at ZBE62.  ·refined
- `B0056:BE53` (x1) — Calls display_string to print alternate status message MBD83.  ·refined
- `B0056:BE60` (x1) — Calls display_string to print message MBD7B at ZBE62.  ·refined
- `B0056:BECB` (x2) — Compares B to 2 for two-channel analog configuration check.  ·refined
- `B0056:BF38` (x2) — This CLRA clears accumulator A before calling a subroutine (ZBF8B) that likely converts a numeric value to a display character, part of formatting a signed number (e.g., for a measurement readout) in the analog board overlay (B0056).

## B0057 — STATE/TIMING configuration menu  (105 functions)

- `B0057:802B` (x1) — This BEQ checks if a flag (M2001) is zero to conditionally load a display list pointer (M5412) for the state/timing configuration menu, part of ROM B0057’s menu rendering logic.
- `B0057:803B` (x2) — This BEQ checks if a flag (M2001) is zero to conditionally load a display list pointer (M5412) for the state/timing configuration menu, part of ROM B0057’s menu rendering logic.
- `B0057:805B` (x1) — Zero the byte counter for a state/timing configuration parameter.
- `B0057:8085` (x2) — Load pointer to a state/timing configuration table (M2A6E).
- `B0057:8095` (x2) — Set A=5 as a parameter for a state/timing configuration call.
- `B0057:80E0` (x4) — Branches if B != 0 to Z80FC, part of a state/timing configuration menu routine (ROM B0057) that selects a display attribute byte (0, 0x0B, or 0x0E) based on a comparison value, likely for CRT cursor or highlight control via the MC6845 CRTC.
- `B0057:8165` (x1) — This LBSR calls subroutine Z3637, part of the state/timing configuration menu ROM (B0057) that handles menu item selection and display updates for the logic analyzer's configuration interface.
- `B0057:818D` (x6) — Load X with address M001D, likely a menu template or string pointer.
- `B0057:81AD` (x1) — Tests memory location M2003 (a flag) to determine whether to call ZD981, likely a subroutine that handles a configuration menu item in the state/timing ROM (B0057), such as updating a display or processing user input.
- `B0057:81B6` (x2) — LEAS -$02,S at Z81B6 allocates two bytes on the stack, used to create local storage for a subroutine that handles a configuration menu item in the state/timing ROM.
- `B0057:81D4` (x1) — entry_81D4 — PSHS A; ... menu helper
- `B0057:8326` (x1) — Stores zero (CLRA/CLRB) into M2683 to initialize a counter or flag used in the state/timing configuration menu's setup of pod or channel parameters.
- `B0057:83E8` (x1) — entry_83E8 — -> LBSR $9B6E
- `B0057:8441` (x3) — Compare A to 1 in state/timing dispatch.
- `B0057:8458` (x1) — This loads the byte from M201D (a mode or state register) into A, checking if it equals 4 or 1 to determine which configuration routine to call (e.g., for different acquisition modes), in the state/timing config menu ROM (B0057).
- `B0057:845E` (x2) — This loads the byte from M201D (a mode or state register) into A, checking if it equals 4 or 1 to determine which configuration routine to call (e.g., for different acquisition modes), in the state/timing config menu ROM (B0057).
- `B0057:848E` (x1) — entry_848E — -> LBSR kernel $D941 (cross-bank call into resident kernel)
- `B0057:8511` (x1) — This LBSR calls ZAE2F, a subroutine in the state/timing configuration menu (B0057) that processes a user input or updates a display field for trigger setup.
- `B0057:8587` (x2) — STA ME812 – Set acquisition control register $E812 (arm trigger).
- `B0057:859D` (x3) — Configures 8279 display mode and clock prescaler for state/timing menu.  ·refined
- `B0057:85F9` (x1) — Writes threshold parameter from M20C2 to acquisition register $E817.  ·refined
- `B0057:8605` (x5) — Sets analog threshold voltage via M2719 ORed with $10 to $E836.  ·refined
- `B0057:8611` (x3) — Computes threshold polarity from M294F for state/timing configuration.  ·refined
- `B0057:86AF` (x1) — Returns from command dispatch handler for state/timing config menu.  ·refined
- `B0057:86DC` (x2) — Sets trigger qualifier bit in acquisition register $E817.  ·refined
- `B0057:8706` (x1) — Resets acquisition engine and arms trigger for capture start.  ·refined
- `B0057:895F` (x2) — Initializes 8279 keyboard/display with 8-bit display mode.  ·refined
- `B0057:8A98` (x2) — Checks state/timing parameter for menu option index calculation.  ·refined
- `B0057:8AD1` (x2) — Tests pod/clock flag M2002 and branches to menu drawing routine.  ·refined
- `B0057:8BC9` (x1) — Call subroutine to update or refresh a display list from configuration.
- `B0057:8D63` (x2) — Entry of subroutine setting acquisition control (ME812) and 8279 command (ME84F).
- `B0057:8D73` (x1) — Tests flag M2A31 to conditionally increment M2A30, part of the state/timing configuration menu (B0057) logic for setting acquisition parameters.
- `B0057:8D7F` (x1) — Tests flag M2A31 to conditionally increment M2A30, part of the state/timing configuration menu (B0057) logic for setting acquisition parameters.
- `B0057:8EC0` (x1) — Write to keyboard/display controller command register ME848.
- `B0057:908B` (x1) — LBSR ZC7F9 at B0057:909F calls a subroutine that processes a display-list template (pointed to by X and U) for the state/timing configuration menu, converting control bytes and text into CRT output via the MC6845.
- `B0057:9097` (x8) — LBSR ZC7F9 at B0057:909F calls a subroutine that processes a display-list template (pointed to by X and U) for the state/timing configuration menu, converting control bytes and text into CRT output via the MC6845.
- `B0057:90DC` (x1) — Load byte from Y pointer (start of pattern buffer in state config).
- `B0057:921B` (x6) — CMPA #$00 – Check menu index (M2739) for zero in BULK2 dispatch.
- `B0057:9223` (x1) — BNE Z9258 – Branch if menu index not one to default handler.
- `B0057:922A` (x1) — BNE Z9258 – Branch if menu index not one to default handler.
- `B0057:9238` (x1) — This LEAX loads the address of string table M9153 (relative to PC), storing it into Z2746 as part of a menu or prompt selection routine that chooses different message strings based on accumulator B in the state/timing config ROM (B0057).
- `B0057:923F` (x1) — This LEAX loads the address of string table M9153 (relative to PC), storing it into Z2746 as part of a menu or prompt selection routine that chooses different message strings based on accumulator B in the state/timing config ROM (B0057).
- `B0057:9254` (x1) — Branches if B is not equal to #$00 (BNE Z9285) as part of a multi-way decision in the state/timing config menu overlay (B0057), selecting between different display list pointers (e.g., M913E, M9175) based on the value in B.
- `B0057:925D` (x1) — Branches if B is not equal to #$00 (BNE Z9285) as part of a multi-way decision in the state/timing config menu overlay (B0057), selecting between different display list pointers (e.g., M913E, M9175) based on the value in B.
- `B0057:9264` (x1) — Branches if B is not equal to #$00 (BNE Z9285) as part of a multi-way decision in the state/timing config menu overlay (B0057), selecting between different display list pointers (e.g., M913E, M9175) based on the value in B.
- `B0057:9299` (x1) — Call kernel subroutine ZD8EF (likely display helper).
- `B0057:92A6` (x1) — Call kernel subroutine ZD8EF (likely display helper).
- `B0057:92B7` (x1) — Return from subroutine; restores registers after saving menu pointer.
- `B0057:92BE` (x1) — Return from subroutine; restores registers after saving menu pointer.
- `B0057:92C5` (x1) — Menu selection dispatch returning via saved pointer in Z274A.  ·refined
- `B0057:9370` (x2) — Checks menu item flags against M2742/M2743 masks for state config.  ·refined
- `B0057:937D` (x4) — Tests analog threshold detection and sets M2744 mode flag to 0 or 2.  ·refined
- `B0057:9410` (x2) — Initializes state/timing config counters M28B6, M266B, M266D, M2757.  ·refined
- `B0057:94DD` (x1) — Loads Y with M91EF table pointer for trigger/clock menu display setup.  ·refined
- `B0057:94EC` (x1) — Loads Y with M91D5 table pointer for trigger/clock menu display setup.  ·refined
- `B0057:953C` (x1) — Copies stack frame to M272E and stores menu pointer to M273A.  ·refined
- `B0057:957A` (x1) — Copies 6 bytes from stack to M272E and advances display list entry.  ·refined
- `B0057:95B2` (x1) — Stores menu selection pointer to M2759 and loads M90EB template address.  ·refined
- `B0057:95CA` (x1) — Loads M90EB template, stores menu pointer, tests M23DA config active flag.  ·refined
- `B0057:961E` (x1) — Initialize state/timing config menu based on M23EE mode flag.  ·refined
- `B0057:966B` (x1) — Initialize second state/timing config menu based on M2400 flag.  ·refined
- `B0057:96B8` (x1) — Initialize third state/timing config menu based on M2413 flag.  ·refined
- `B0057:96C3` (x1) — Process mode-6 special-case config tables M241E and M2420.  ·refined
- `B0057:9704` (x1) — Calculate capture data range end address from M2691 depth.  ·refined
- `B0057:97BD` (x2) — Write delay count to analog threshold DAC at $E838.  ·refined
- `B0057:9835` (x2) — Copy 11-byte config block from M2546 to M26BE workspace.  ·refined
- `B0057:9A77` (x1) — Compare pointer against M03FC for state/timing menu exit.  ·refined
- `B0057:9A99` (x1) — Compare string at M9831 and update threshold via ZAE2F/ZCA72.  ·refined
- `B0057:9AA2` (x1) — Compare string at M982D and update threshold M269B via ZC5A7.  ·refined
- `B0057:9B6E` (x1) — This CMPB #$02 checks if the value at M2083 equals 2, which is part of configuring the analog/threshold subsystem via $E836, likely setting a threshold or gain parameter for the acquisition hardware.
- `B0057:9BBE` (x1) — Load destination address of trigger pattern buffer at M26F9.
- `B0057:9BEC` (x1) — Subtract start address from M2645 to get buffer length.
- `B0057:9C53` (x4) — This pushes registers U, X, and D onto the stack, saving context at the start of a subroutine that processes configuration data (e.g., state/timing parameters) based on a flag at stack offset $04, in the state/timing config menu ROM (B0057).
- `B0057:9CB1` (x5) — This transfers U to X to set up a pointer for a subsequent subroutine call (FCB+$95) that formats or processes a menu item, within a state/timing configuration menu handler in B0057.
- `B0057:9CBD` (x5) — This transfers U to X to set up a pointer for a subsequent subroutine call (FCB+$95) that formats or processes a menu item, within a state/timing configuration menu handler in B0057.
- `B0057:9CC9` (x5) — This transfers U to X to set up a pointer for a subsequent subroutine call (FCB+$95) that formats or processes a menu item, within a state/timing configuration menu handler in B0057.
- `B0057:9CE1` (x1) — Calls ZC5F7 to display formatted menu line.
- `B0057:9D4D` (x1) — This loads Y with #MC030 (a CRTC register address) to set up the CRT controller for a display format, part of the state/timing configuration menu (ROM B0057) that initializes video parameters.
- `B0057:9E84` (x1) — Branch to next option comparison if trigger qualifier index != 1.
- `B0057:9F17` (x1) — Test flag at M25F2 (related to state/timing configuration).
- `B0057:9F40` (x1) — This subtracts two 16-bit values (M2645 from M2647), computing a difference used in a timing or position calculation, likely part of a state/timing configuration menu routine that calculates delays or intervals.
- `B0057:9FAF` (x1) — This loads a constant #$0028 (40 decimal) as a delay count, part of a timing loop for the CRTC or keyboard/display subsystem initialization.
- `B0057:9FC6` (x1) — Reload 40 decimal delay count for another CRTC timing loop.
- `B0057:A38E` (x1) — prints "Label>"
- `B0057:ACC1` (x1) — prints "Edge"
- `B0057:ACE4` (x1) — prints "Edge"
- `B0057:ACF4` (x1) — prints "after x"
- `B0057:ADB9` (x3) — prints "Equals"
- `B0057:B22D` (x1) — prints "In sequence"
- `B0057:B23E` (x1) — Prints "find Master" using display-string template with control bytes.  ·refined
- `B0057:B279` (x1) — Prints "find" using display-string template with control bytes.  ·refined
- `B0057:B27E` (x2) — Prints "then" using display-string template with control bytes.  ·refined
- `B0057:B297` (x1) — Prints "while storing" using display-string template with control bytes.  ·refined
- `B0057:B2C8` (x1) — Prints "then" using display-string template with control bytes.  ·refined
- `B0057:B2E3` (x1) — Prints "Trace at" using display-string template with control bytes.  ·refined
- `B0057:B2E9` (x1) — Prints "Start Timer at" using display-string template with control bytes.  ·refined
- `B0057:B30E` (x1) — Prints "Occurrences of" using display-string template with control bytes.  ·refined
- `B0057:B390` (x1) — Prints "while storing" using display-string template with control bytes.  ·refined
- `B0057:B3B3` (x1) — Prints "while storing" using display-string template with control bytes.  ·refined
- `B0057:B3DA` (x1) — prints "then store"
- `B0057:B40A` (x1) — prints "Sequence Restart on"
- `B0057:B428` (x1) — prints "Stop Timer on"
- `B0057:B986` (x1) — prints "Ranges"
- `B0057:BA24` (x1) — prints "thru"
- `B0057:BA52` (x1) — prints "<= Range"

## B0058 — ACQUISITION + listing display  (153 functions)

- `B0058:8101` (x1) — Call subroutine at ZEB82 (CRTC cursor positioning or I/O write).
- `B0058:8167` (x1) — Branches if A < 2, part of a range-checking routine that validates a command or mode byte (e.g., acquisition type) before dispatching to handler code.
- `B0058:82A1` (x1) — Unconditional long branch (LBRA Z99D7) to a routine in the acquisition overlay (B0058), likely part of a jump table or control flow for processing acquisition data or display updates.
- `B0058:83A4` (x1) — This tests flags M2FED and M29D3 to determine whether to call subroutine ZABB8 (likely a data processing or display update routine) before returning from a subroutine.
- `B0058:83B5` (x2) — Save U, X, A for indexed display string routine.
- `B0058:8449` (x1) — This DECB decrements the loop counter (initialized to #$06) in a zero-fill loop that clears 6 bytes on the stack, part of a local variable initialization in the acquisition/listing overlay (B0058).
- `B0058:84C6` (x1) — Test D sign for signed offset in listing routine.
- `B0058:8510` (x3) — CMPA ME031: Compare A with I/O register $E031 (CRTC or threshold).
- `B0058:8630` (x1) — Load D with $0800 (bit pattern for testing trigger qualifier bits).
- `B0058:8688` (x1) — Return from HP-IB configuration menu display routine.
- `B0058:86EA` (x2) — This BNE branches to Z86FB if the byte at M201D is not #$01, selecting between two different pointer values (M0001 vs M0353) for a linked-list traversal in the acquisition/listing ROM (B0058).
- `B0058:8733` (x3) — prints "DATA ADDR STAT 4v2~3"
- `B0058:8755` (x1) — prints "DATA ADDR STAT 4v2~3"
- `B0058:879B` (x1) — This loads A from M2779 and compares it to #$03, checking a mode or state flag to decide whether to call a subroutine (Z8755) in the acquisition+listing ROM (B0058).
- `B0058:8828` (x2) — Selects timing vs state acquisition path based on mode byte and bank.  ·refined
- `B0058:8864` (x1) — Computes index into parameter table M20F8 or M2086 based on mode flag.  ·refined
- `B0058:89C0` (x4) — Tests mode byte and flag M2001 to conditionally skip display processing.  ·refined
- `B0058:8A19` (x1) — Conditionally outputs carriage return based on Z9661 result and flag M2A36.  ·refined
- `B0058:8B5C` (x1) — Displays string from indirect table M8AC3 indexed by M201D, saves cursor to M2DAE.  ·refined
- `B0058:8B69` (x1) — Displays string from indirect table M8AC3 indexed by M201D, saves cursor to M2DAE.  ·refined
- `B0058:8B9C` (x2) — Increments counter at M2A32+B, calls ZEB82 if counter was zero.  ·refined
- `B0058:8BAC` (x2) — Decrements counter at M2A32+B, calls ZEB82 when counter reaches zero.  ·refined
- `B0058:8CEB` (x1) — Copies acquisition data to display buffer using format table M2697 or M2699.  ·refined
- `B0058:8D25` (x1) — Copies display data from X to U using format table pointer from Z8D33.  ·refined
- `B0058:8D33` (x2) — Load pointer to state/timing format table at $2637.
- `B0058:8D69` (x1) — This CLRA/CLRB clears the D-register to return a zero value from a subroutine in the acquisition+listing ROM (B0058), used as a fallback when a prior call (ZA7F9) returns non-zero, indicating a failure or empty result.
- `B0058:8D74` (x1) — Branch if sample count <= threshold to normalization path.
- `B0058:8DEE` (x1) — Load immediate value $2300 (likely a RAM buffer address for listing).
- `B0058:8DFC` (x1) — Call subroutine to populate listing buffer with acquisition data.
- `B0058:8E13` (x1) — Branch on user response from status prompt M8D59 to call listing display.
- `B0058:8E1C` (x1) — Call subroutine Z8D74 to display listing data and wait for key.
- `B0058:8E2F` (x2) — LBSR ZA5F7 call to a subroutine that processes a 6-byte table entry indexed by B, used for decoding or formatting acquisition data in ROM B0058.
- `B0058:8E4D` (x2) — This instruction compares A to 0, serving as the entry point for a dispatch routine that selects one of three display strings (M8D5D, M8D61, or another) based on the value in A, used in the acquisition/listing UI (ROM B0058).
- `B0058:8E53` (x1) — Save X and A on stack for field formatting routine.
- `B0058:8E5D` (x1) — Save X and A on stack for field formatting routine.
- `B0058:8E66` (x1) — Load X with constant 6 for field width/format argument.
- `B0058:8E6D` (x1) — This loads the constant 7 as an argument for a subsequent subroutine call (Z1637), part of a routine that processes or formats data fields indexed by small integers in the acquisition/listing overlay (B0058).
- `B0058:8EA4` (x1) — This BRA instruction creates a loop that increments X and calls Z1637 (a delay or polling routine) for each value of A from 8 to 10, likely part of a timing calibration or acquisition setup loop in the acquisition+listing ROM (B0058).
- `B0058:8EC3` (x2) — Call to initialize checksum accumulator before block write.
- `B0058:8F25` (x1) — Call subroutine at Z1AA5 (likely checksum update)
- `B0058:8F6F` (x1) — Call subroutine at Z1864 (acquisition or display routine)
- `B0058:9076` (x3) — Load X with address of table M17CC (character/attribute data)
- `B0058:90C1` (x2) — Branch if index < limit (valid selection) for string table lookup
- `B0058:9142` (x1) — Load X from M0DA4 (pointer to list of trigger/state descriptors).
- `B0058:917D` (x1) — Compares A with the value at stack offset $05, part of a search/selection algorithm that finds the best matching entry (e.g., trigger level or threshold) within a linked list or table.
- `B0058:91B8` (x1) — "Check for command value 2; if match, proceed to load acquisition parameters M2779/M201D."
- `B0058:9447` (x4) — Load byte at offset 3 of X (likely a limit or count value).
- `B0058:948F` (x2) — Load RAM variable M202F (likely a configuration parameter)
- `B0058:951A` (x1) — Load byte from RAM variable M2779 (likely menu/state index)
- `B0058:9548` (x1) — Comparison of A with #$04 to determine processing path for different data types or modes in the acquisition/listing overlay
- `B0058:9583` (x1) — Compare A with byte at M2779 (likely a mode/state variable).
- `B0058:9661` (x4) — Call display/CRTC subroutine at ZEB82
- `B0058:9670` (x1) — This clears A and B (CLRA/CLRB) to zero out a 16-bit field at offset $04 of a structure pointed to by X, used as an initial value in an acquisition parameter calculation in B0058.
- `B0058:9676` (x1) — This clears A and B (CLRA/CLRB) to zero out a 16-bit field at offset $04 of a structure pointed to by X, used as an initial value in an acquisition parameter calculation in B0058.
- `B0058:97D4` (x1) — Load constant #MFF9C into D, part of a multi-way dispatch (B0058) that selects a base address or offset based on a mode byte (B), likely for acquisition buffer or timing calculations.
- `B0058:9835` (x1) — This compares B to #$05, part of a multi-way branch in the acquisition+listing ROM (B0058) that handles different data formats or display modes (e.g., for listing or waveform).
- `B0058:994D` (x1) — Allocate stack frame for local variables in number formatting routine.
- `B0058:99E3` (x1) — Load X with a PC-relative address (M970B), part of the acquisition/listing ROM (B0058) routine that selects a display string or format table based on a flag (bit 6 of B), used in listing or waveform display generation.
- `B0058:99F2` (x1) — This returns from subroutine, restoring PC, U, X, and D registers, exiting a display or data formatting routine in the acquisition/listing overlay.
- `B0058:99F8` (x1) — This returns from subroutine, restoring PC, U, X, and D registers, exiting a display or data formatting routine in the acquisition/listing overlay.
- `B0058:9A0C` (x3) — Call output routine Z1AA5 to display character.
- `B0058:9A28` (x2) — This instruction loads accumulator A from offset 2 of a structure pointed to by X, which is part of a BCD addition routine (using DAA) that likely converts or adjusts a binary counter to BCD format for display in the acquisition/listing ROM (B0058).
- `B0058:9A58` (x2) — LBSR ZB5D9 - This calls a subroutine in the acquisition+listing ROM (B0058) that likely performs data validation or range checking on captured acquisition data before display or storage.
- `B0058:9AD9` (x1) — Calls subroutine ZB7D4 to perform a display or data manipulation operation after storing a byte to a structure, part of the acquisition/listing overlay (B0058) for formatting captured data.
- `B0058:9AEE` (x1) — This calls subroutine Z9F6A with the carry flag set (SEC) in the acquisition+listing ROM (B0058), initiating a bit-serial data transfer or shift operation to extract captured logic analyzer samples from the acquisition FIFO.
- `B0058:9F6A` (x3) — Rotates B left by one bit, extracting the most significant bit for a flag test in a subroutine that processes acquisition data or trigger status bits.
- `B0058:9FE7` (x2) — Long branch to subroutine ZA13A (acquisition/listing handler).
- `B0058:A02D` (x4) — Load pointer to buffer M28D7 (zero-case acquisition data).
- `B0058:A13A` (x1) — This CLR $10,S clears a local variable on the stack, likely initializing a flag or counter used in the acquisition listing (B0058) routine that processes captured data for display.
- `B0058:A24B` (x1) — "Clear A for ZD230 zero-byte separator in listing formatting."
- `B0058:A27A` (x1) — This LDD #M0004 loads the constant 4 into D-register as part of a switch-case structure in the acquisition+listing ROM (B0058) that maps a mode value (0-5) to a corresponding parameter (e.g., 4 for mode 3), likely for setting a timing or count value.
- `B0058:A330` (x1) — This LBSR calls subroutine ZA02D, which processes acquisition parameters or triggers, storing results into M28D7 and comparing against M294A to set a flag (M294C) for state/timing configuration in ROM B0058.
- `B0058:A3B5` (x2) — Part of waveform coordinate conversion: subtract constant M00C8 (200).
- `B0058:A3F2` (x2) — This calls subroutine ZA46B in the acquisition+listing ROM (B0058) to process a second byte from M28E8, part of a validation routine that computes and compares two results (likely trigger timing parameters) for consistency checking.
- `B0058:A46B` (x2) — Validates display format parameter B against bounds 5-63.  ·refined
- `B0058:A47D` (x2) — Initializes local stack flag for STATE mode acquisition processing.  ·refined
- `B0058:A550` (x1) — Copies two channel groups then selects data source by mode.  ·refined
- `B0058:A55E` (x1) — Fetches two acquisition bytes and checks buffer pointer match.  ·refined
- `B0058:A58A` (x2) — Selects data source pointers based on mode flag M201D.  ·refined
- `B0058:A5A7` (x1) — Branches to common continuation after mode-based pointer selection.  ·refined
- `B0058:A5F7` (x2) — Loops to fill buffer with modified bytes until counter zero.  ·refined
- `B0058:A6E4` (x1) — Increments list depth counter and loads sub-list pointer.  ·refined
- `B0058:A79B` (x3) — Formats display field with space or data byte based on threshold.  ·refined
- `B0058:A7A1` (x1) — Converts byte to ASCII and selects blank or data for display.  ·refined
- `B0058:A7F9` (x1) — Copies kernel config table via Z43D5, then indexes by stack mode to fetch byte.  ·refined
- `B0058:A855` (x1) — Tests M2F7D, switches bank $0B, sets up stack frame for display/menu.  ·refined
- `B0058:A89D` (x2) — Error handler displaying ZA84B, then selects label table (M26BE/M26C9) via M2F7E.  ·refined
- `B0058:A8CE` (x1) — Validates sample count Y in [1,126], sets error flag at $12,S for listing.  ·refined
- `B0058:A8FC` (x1) — Displays out-of-bounds error message, or loads label table and calls ZA987 formatter.  ·refined
- `B0058:A931` (x1) — Dispatches on stack flags $11,S/$12,S to error string or measurement format via Z6823.  ·refined
- `B0058:A9BA` (x1) — Selects table MA665/MA661/MA669 into U based on stack parameter $08,S, calls Z69DF.  ·refined
- `B0058:A9C4` (x1) — Parameter=1 path: loads table MA661 into U, branches to Z69DF display routine.  ·refined
- `B0058:A9CE` (x1) — Parameter=2 path: loads table MA669 into U, falls through to Z69DF display routine.  ·refined
- `B0058:A9DA` (x1) — Writes display list constants ($06C0/$0180 or $0280) to U based on command byte from X.  ·refined
- `B0058:A9DF` (x2) — Initialize display list buffer with parameters for listing mode.  ·refined
- `B0058:AA6C` (x1) — Select and display one of three status strings based on stack parameter.  ·refined
- `B0058:AA72` (x3) — Select and display one of three status strings based on stack parameter.  ·refined
- `B0058:AA7D` (x1) — Branch to display second string if parameter equals 1.  ·refined
- `B0058:AA87` (x1) — Branch to display third string if parameter equals 2.  ·refined
- `B0058:AAA0` (x6) — Compare acquisition mode at M201D to 2 for timing flag.  ·refined
- `B0058:AAA6` (x1) — Print "Off" and set timing flag based on acquisition mode at M201D.  ·refined
- `B0058:AB2A` (x1) — Convert BCD value to ASCII digit and output via character routine.  ·refined
- `B0058:AB33` (x2) — Convert hex nibble to ASCII digit and output via character routine.  ·refined
- `B0058:AB63` (x1) — Initialize listing format table pointer based on mode at M201D.  ·refined
- `B0058:AC32` (x1) — Builds linked list of captured events in acquisition buffer.  ·refined
- `B0058:AC93` (x2) — Selects display symbol 0x26 for non-matching acquisition markers.  ·refined
- `B0058:AEC3` (x1) — Checks acquisition counter at M28F5 against limit of 10.  ·refined
- `B0058:AFE4` (x1) — Increments vertical position counter M2DC5 for display update.  ·refined
- `B0058:B08C` (x1) — Prints "On Acquired Data Place Cursors:" prompt via display_string.  ·refined
- `B0058:B096` (x1) — Prints "On First Occurrence of" prompt via display_string.  ·refined
- `B0058:B0E8` (x1) — Prints "On All Occurrences of" prompt via display_string.  ·refined
- `B0058:B0F2` (x1) — Loads X with M23DA for printing listing label string.  ·refined
- `B0058:B156` (x2) — Checks config at M20C7+1 to set display mode 0 or 2.  ·refined
- `B0058:B190` (x4) — Tests M2006 flag for zero to skip B=5 case in state machine.  ·refined
- `B0058:B2A4` (x1) — Loads A from the byte pointed to by X (a data structure for acquisition parameters) to check its value for branching to different listing or display modes in the acquisition/listing overlay.
- `B0058:B2FD` (x1) — This LBSR calls ZB156, a subroutine in the acquisition+listing ROM (B0058) that processes or formats a line of captured data, using X as a pointer (incremented by 2 from the stack parameter).
- `B0058:B313` (x1) — prints "And Glitch"
- `B0058:B342` (x1) — This LDX loads a pointer from the stack, part of a routine that processes acquisition data by advancing to a specific offset (0x0A) for further processing in the acquisition+listing overlay.
- `B0058:B3C6` (x1) — prints "Stop Continuous Runs"
- `B0058:B409` (x1) — prints "Pattern & Glitch Definition:"
- `B0058:B412` (x1) — prints "Label>"
- `B0058:B421` (x1) — prints "Base >"
- `B0058:B42E` (x1) — Test flag at RAM $2080 (acquisition mode or state indicator).
- `B0058:B5B0` (x2) — BRA branches to ZB5CE (which clears I/O registers ME030 and ME02E) when a comparison fails, likely aborting a display formatting loop and resetting the CRTC or acquisition control.
- `B0058:B5D8` (x9) — This calls subroutine Z5AA5 with A=0x47 ('G'), likely to output a character to the display or serial port, part of a routine that prints a label or prompt in the acquisition/listing overlay.
- `B0058:B5E6` (x9) — This calls subroutine Z5AA5 with A=0x47 ('G'), likely to output a character to the display or serial port, part of a routine that prints a label or prompt in the acquisition/listing overlay.
- `B0058:B60B` (x1) — This instruction loads the X register from a stack frame slot during a display formatting routine in the listing ROM (B0058), retrieving a pointer to a data structure for rendering measurement results.
- `B0058:B620` (x2) — LEAX $03,X advances a pointer by 3 bytes, part of a display formatting routine in B0058 that traverses a linked list of data structures for rendering measurement results.
- `B0058:B637` (x1) — This stores accumulator A to I/O register $E02E (STA ME02E) to configure a hardware parameter in the acquisition/timing engine, part of a setup routine in ROM B0058.
- `B0058:B6EA` (x1) — LBSR ZD94D - This calls a subroutine in the acquisition+listing ROM (B0058) that likely handles display output or data formatting for the listing display, as part of the acquisition data processing pipeline.
- `B0058:B74E` (x2) — load pointer to second dispatch table
- `B0058:B805` (x1) — Store the address of a data table (MB800) onto the stack, preparing parameters for a subsequent call to ZD076; this is part of a listing or acquisition data-processing routine in B0058.
- `B0058:B854` (x1) — Increment loop counter for acquisition state machine.
- `B0058:B8B8` (x1) — prints "Pod1"
- `B0058:B914` (x1) — Load parameter 3 for kernel display routine.
- `B0058:B941` (x3) — Call kernel routine at $D94D, likely display list helper
- `B0058:B9E7` (x1) — Call kernel subroutine $F5D9 (likely display or process menu item).
- `B0058:BB86` (x1) — This instruction adjusts the stack pointer by 4 bytes to clean up local variables in a subroutine from the acquisition/listing ROM (B0058), likely part of a display list rendering function.
- `B0058:BB9A` (x1) — Load Y with address of constant table MCC81 (display attribute data).
- `B0058:BBD8` (x2) — prints "than"
- `B0058:BC47` (x1) — Load address of constant table MCC81 for display formatting.
- `B0058:BE00` (x1) — This loads D with the immediate value 0x0180 (LDD #M0180), which is then stored into the sample clock register (ME806 at $E806) and another register (ME80C at $E80C), part of a routine in the acquisition/listing overlay (B0058) that configures the acquisition timing (sample rate or clock divider).
- `B0058:BE7A` (x1) — prints "Acquisition ok"
- `B0058:BEAE` (x1) — This jump-to-subroutine at address 0x4D20 (in DRAM work RAM) executes a dynamically-loaded routine, likely a callback or overlay handler for acquisition or listing operations in ROM B0058.
- `B0058:BED2` (x1) — This instruction clears the I/O register at $E8A0 (part of the acquisition engine control) during a reset/initialization sequence in the listing ROM (B0058), disabling the acquisition hardware.
- `B0058:BF2D` (x1) — prints "HP-IB failed loop back test"
- `B0058:BF34` (x1) — JSR     ZF54F                    *BF38: Call display_string routine at $F54F to print message
- `B0058:BF43` (x2) — Tests bit 4 of the capture done register (ME854) to check if the acquisition FIFO has data, part of the capture completion polling routine.
- `B0058:BF5C` (x6) — This stores the result of an XOR/OR accumulation back to the output buffer, part of a data compression or pattern-matching algorithm (e.g., run-length encoding or checksum) in the acquisition overlay.
- `B0058:BF82` (x1) — Store A to sample clock register $E806 (start/stop acquisition)
- `B0058:BF94` (x3) — Store A to sample clock register $E806 (start/stop acquisition)
- `B0058:BF9E` (x6) — Load MC6845 register index from CRTC address port $C604

## B0059 — MEASUREMENT UI — histogram / waveform / statistics  (185 functions)

- `B0059:8011` (x1) — LDA loads #$20 (space character) as a default display character, which may be replaced by #$5B ('[') depending on a flag, for formatting a measurement or status line in the UI.
- `B0059:804A` (x2) — LEAX M7A8C,PCR — Load address of formatting table for bracketed field display.
- `B0059:8107` (x1) — Unconditional long branch to Z82CC, serving as a return or exit point from a measurement UI subroutine (ROM B0059), likely after processing histogram or waveform data.
- `B0059:811C` (x1) — LBSR to Z8330, a subroutine for formatting or converting measurement data.
- `B0059:8125` (x2) — Load B from indexed table (X+A), fetching a formatting parameter.
- `B0059:81FE` (x1) — Load address of measurement type table M7E06 (relative).
- `B0059:829C` (x1) — Loads the effective address of a string table (M7FE3) into X using PCR-relative addressing, part of a routine in the measurement UI ROM (B0059) that formats or displays numeric values (e.g., converting to ASCII).
- `B0059:82E6` (x1) — Load address M7AEE, a table for waveform display characters.
- `B0059:8330` (x1) — Clear one byte of display attribute buffer, increment pointer.
- `B0059:8439` (x2) — This INCB increments the B register in a loop that initializes memory from #M2000, likely zeroing or setting up a work buffer for measurement data processing in the measurement UI ROM.
- `B0059:859B` (x4) — Branch if display list parser returned non-zero
- `B0059:85A7` (x2) — Branch if display list parser returned non-zero
- `B0059:85F7` (x3) — This clears both accumulators (CLRA/CLRB) in the measurement UI ROM (B0059), then subtracts the value on the stack from zero to compute the two's complement negation, part of a signed arithmetic routine for waveform or histogram data normalization.
- `B0059:8601` (x7) — This clears both accumulators (CLRA/CLRB) in the measurement UI ROM (B0059), then subtracts the value on the stack from zero to compute the two's complement negation, part of a signed arithmetic routine for waveform or histogram data normalization.
- `B0059:8610` (x1) — This is a conditional branch (BNE) that checks the result of a previous subroutine call (Z87A1), determining whether to clear fields in a data structure (U offsets) or continue processing, part of a measurement or display routine in the measurement UI ROM (B0059).
- `B0059:8649` (x1) — Branch back if loop counter not zero (continue iteration)
- `B0059:86FB` (x1) — PSHS U,X,D - Save registers for BCD formatting subroutine entry.
- `B0059:8714` (x1) — Loop back while counter non-zero (9-bit waveform processing).
- `B0059:8739` (x2) — Rotate left through carry the byte at offset 2 of a data structure, part of a multi-byte shift or multiplication algorithm used in waveform data scaling or histogram bin accumulation.
- `B0059:8774` (x2) — Add 16-bit value from U+2 to D (arithmetic on measurement data).
- `B0059:8788` (x8) — This instruction checks if the increment of a 24-bit counter at X+2 caused a carry, part of a multi-byte increment routine used for timestamp or acquisition counter arithmetic in ROM B0059.
- `B0059:87D4` (x5) — Multi-byte left shift of 4-byte value at X+1 through X+4.  ·refined
- `B0059:87DF` (x7) — Multi-byte right shift of 4-byte value at X+1 through X+4.  ·refined
- `B0059:880C` (x10) — ORs three status bytes at X+1..X+3, returns Z flag set if all zero.  ·refined
- `B0059:8819` (x1) — Displays error message via display_string if status flags nonzero.  ·refined
- `B0059:8823` (x1) — Toggles byte at X, then calls Z880C to check resulting flags.  ·refined
- `B0059:882F` (x2) — Increments byte at X, then arithmetic right shifts 4-byte value at X+1.  ·refined
- `B0059:8843` (x3) — Two's complement negates 4-byte value at X+1 through X+4.  ·refined
- `B0059:889D` (x4) — Saves D and decrements stack pointer (stack frame setup).  ·refined
- `B0059:88D5` (x8) — Parses two arguments from stack, stores results in local buffer.  ·refined
- `B0059:8996` (x3) — Increments 3-byte counter at X+1..X+3 if bit 7 of X+4 is set.  ·refined
- `B0059:89DF` (x3) — Waveform pixel saturation: branch if positive on overflow.
- `B0059:8C11` (x1) — This branch (BRA Z8C4A) is part of a switch-case structure that maps an input value (0–4) to a constant, likely for selecting a display mode or measurement type in ROM B0059.
- `B0059:8DC1` (x1) — This loads U with the address of work RAM variable M0221, preparing to call a display formatting routine that renders measurement data (e.g., histogram or waveform labels).
- `B0059:8DCD` (x1) — Call subroutine Z9099 (measurement data display renderer).
- `B0059:8DD9` (x1) — prints "those not in any Range"
- `B0059:8E02` (x1) — prints "those not in any Range"
- `B0059:8E0D` (x1) — This LBSR calls subroutine Z880C, which likely performs a string comparison or menu navigation check, part of the measurement UI overlay (B0059) for histogram/waveform display.
- `B0059:8E26` (x1) — This LEAS instruction adjusts the stack pointer to clean up local variables and prepare for a subroutine return, part of a routine in the acquisition/listing overlay (ROM B0058).
- `B0059:8E2E` (x1) — Long branch to menu item handler Z8EE6 on match.
- `B0059:8E6F` (x1) — Branch if measurement count non-zero to skip default minimum set.
- `B0059:8E7D` (x1) — Stores the 16-bit result from subroutine Z85A7 into stack offset $0B, used in a measurement calculation (likely a time interval or count) in the measurement UI overlay (B0059).
- `B0059:8F12` (x1) — This instruction stores a byte from A into memory pointed by U (post-increment), copying a 5-byte template (from Z8D38) into a work buffer (M2F21 area), part of a routine in B0059 that initializes a display or measurement data structure.
- `B0059:8FCF` (x1) — This STD stores a 16-bit value from the U pointer into offset 3 of a linked-list node at X, part of a data structure traversal in the measurement UI overlay (B0059) for processing waveform or histogram data.
- `B0059:8FEF` (x1) — Calls subroutine to process measurement UI template (M2DDE) for display.
- `B0059:906B` (x1) — Call subroutine Z89DF, probably a display or formatting routine.
- `B0059:9075` (x1) — Call subroutine Z89DF, probably a display or formatting routine.
- `B0059:907F` (x1) — Clear byte at offset 8 in stack frame, probably a counter or flag.
- `B0059:9099` (x6) — Calls subroutine Z8439 (a local routine), part of a routine that sets up a stack frame and clears a byte at offset 9 of U, likely initializing a data structure for waveform or histogram computation in the measurement UI ROM (B0059).
- `B0059:91B8` (x1) — Conditional branch based on signed comparison; decide whether to display a measurement value.
- `B0059:9216` (x1) — This STB ,S stores a byte from M2A32 (a keyboard scan code or state) onto the stack, part of the measurement UI (ROM B0059) that processes keypresses for waveform/histogram interaction.
- `B0059:92A8` (x1) — prints "Acquisition Time:"
- `B0059:92F3` (x1) — Load pointer to display template at M2572 for second menu
- `B0059:9318` (x1) — STA stores #$4A ('J') into offset $08 of a structure pointed by X, setting a display attribute or menu item code when M2669 is non-zero and M2779 equals 4, for a measurement UI option.
- `B0059:9371` (x2) — Combines character attribute from mapping table with stack display flags for output via ZFAA5.
- `B0059:93BB` (x1) — Increment B (INCB), part of a routine in the measurement UI overlay (B0059) that clamps a value between bounds stored in a data structure (X), likely for adjusting a display position or measurement index.
- `B0059:9428` (x4) — Call subroutine Z6510 to process or format measurement data for display.
- `B0059:94B0` (x2) — Histogram display routine counting consecutive non-zero data samples and writing bar start pattern.
- `B0059:9570` (x2) — Load Y with address of display format table M2D32.
- `B0059:95E8` (x14) — uic_95E8 — LBSR'd after uic_969F with a table ptr — render/format a UI field
- `B0059:9667` (x1) — Increment measurement point counter in processing loop.
- `B0059:968C` (x2) — Multiply to calculate offset into measurement data buffer.
- `B0059:969F` (x13) — uic_969F — alloc/clear: PSHS U,X,D; MUL; ADDD #8; clear loop; returns X=buffer
- `B0059:9738` (x1) — Store page counter on stack for MMU mapping loop
- `B0059:9771` (x1) — Load D with count 4 (number of CRTC registers to program)
- `B0059:977C` (x1) — Load D with CRTC mode control value $2003 (cursor/display mode)
- `B0059:97D4` (x4) — Store pointer M0000 into variable M2775 (measurement UI buffer).
- `B0059:9813` (x1) — Branch if measurement state flag is zero (skip cleanup)
- `B0059:9896` (x1) — Load two-byte value from kernel RAM (e.g., threshold or limit).
- `B0059:9B26` (x1) — Range-checks A/B against measurement bounds, sets display attribute on 0x3F match.  ·refined
- `B0059:9B34` (x1) — Compares B to 0x3F, branches to set display attribute or increment cursor.  ·refined
- `B0059:9B3B` (x1) — Calls display attribute setter Z71B8, then increments measurement UI cursor.  ·refined
- `B0059:9BAD` (x1) — Compares U to STATE/TIMING config table addresses, dispatches mode handler.  ·refined
- `B0059:9BD8` (x5) — Writes 0x0C to analog threshold I/O port $E836 after trigger capture.  ·refined
- `B0059:9C30` (x1) — Loads overlay bank 2, stores display list template pointer into structure.  ·refined
- `B0059:9C64` (x1) — Increments offset 7 in display structure, follows linked list, stores pointer.  ·refined
- `B0059:9C6F` (x1) — Stores pointer and increments offset 7 in linked display structure.  ·refined
- `B0059:9C97` (x1) — Clamps menu index B to 0-3, stores to structure, calls measurement UI update.  ·refined
- `B0059:9CC2` (x1) — Selects axis label string table based on A, outputs character via kernel.  ·refined
- `B0059:9CC9` (x1) — This loads the character '[' ($5B) with bit 7 set (OR #$80) for display, part of the measurement UI overlay (B0059) that formats a histogram or waveform axis label with highlighted bracket characters.
- `B0059:9D66` (x2) — Call subroutine at $CB82 (probably a character output or display driver)
- `B0059:9D7C` (x1) — Loads B from stack offset 1 (LDB $01,S) to retrieve a parameter for comparison, part of a routine in the measurement UI ROM (B0059) that selects display data or string pointers based on a mode flag.
- `B0059:9D89` (x3) — Loads B from stack offset 1 (LDB $01,S) to retrieve a parameter for comparison, part of a routine in the measurement UI ROM (B0059) that selects display data or string pointers based on a mode flag.
- `B0059:9DDD` (x2) — Branch if not equal to skip histogram/waveform adjustment.
- `B0059:9F20` (x1) — Load A from M2546 (second measurement parameter for mode 1).
- `B0059:A2EA` (x1) — Return from BULK2 subroutine copying kernel data to display buffer.
- `B0059:A302` (x3) — This instruction stores the value $0C into a control variable at $2F4F, part of a routine in ROM B0059 that sets up parameters for a measurement overlay display (likely histogram or statistics) via the display-list template system.
- `B0059:A320` (x1) — Return from waveform display setup subroutine (sets M2F4F etc., calls ZC3D5).
- `B0059:A332` (x1) — Return from measurement mode display setup subroutine (sets M2F4F etc., calls ZC3D5).
- `B0059:A34A` (x1) — LDA loads #$0C to set M2F4F as a parameter for a subsequent call to ZC3D5, configuring a timer or counter value for a measurement or display update routine in the measurement UI overlay.
- `B0059:A37A` (x1) — This loads X with #$0018, setting up parameters for a subroutine call (ZC3D5) that likely initializes or configures a timer or counter, part of a measurement UI routine that manages display update intervals.
- `B0059:A392` (x2) — Load value $0C (12) into A for measurement parameter count.
- `B0059:A3AA` (x2) — Load value $0C (12) into A for measurement parameter count.
- `B0059:A3C2` (x2) — Load value $0C (12) into A for measurement parameter count.
- `B0059:A401` (x1) — Stores #$0C into M2F4F to set the number of lines or a row count for a waveform display region, used in the measurement overlay's histogram/waveform rendering routine.
- `B0059:A40C` (x1) — Stores #$0C into M2F4F to set the number of lines or a row count for a waveform display region, used in the measurement overlay's histogram/waveform rendering routine.
- `B0059:A424` (x5) — Calls subroutine ZC3D5 (likely a kernel routine for display or menu handling) after setting up parameters (M2F4F, M2F50, M2F52) for a display-list operation in the measurement UI ROM (B0059).
- `B0059:A477` (x1) — Loads X with immediate address $001D (LDX #M001D) as a parameter for a subsequent call to Z5637, part of a display or menu update routine in the measurement UI ROM (B0059).
- `B0059:A56B` (x1) — Compare A with 2 to set measurement mode flag on stack.
- `B0059:A59E` (x1) — LBSR ZB458 at ZA5B9 calls a subroutine to update display elements when a stack flag is zero, part of a measurement UI routine that conditionally refreshes the screen in B0059.
- `B0059:A669` (x1) — Call subroutine at Z2B82 (keyboard/display handler for 8279 controller).
- `B0059:A6AE` (x3) — Call subroutine ZA332 (measurement display update).
- `B0059:A6E8` (x1) — Call display character routine at Z5AA5 (CRTC output).
- `B0059:A756` (x1) — prints "Run"
- `B0059:A7A4` (x2) — prints "Hit"
- `B0059:A7F8` (x2) — Branch to skip TIMING mode check after STATE trigger.
- `B0059:A88E` (x1) — Increments stack byte at offset 7 (statistics row counter).
- `B0059:A93F` (x1) — This LDU $03,S in the measurement overlay (B0059) loads a pointer from the stack (offset 3) used for accessing a data structure or table, part of a routine that processes acquisition data for histogram/waveform display, conditional on a flag at $09,S.
- `B0059:A998` (x1) — This instruction subtracts #$0100 from D (SUBD #M0100), likely decrementing a counter or adjusting a memory pointer in the measurement UI ROM (B0059) for histogram/waveform processing.
- `B0059:A9A1` (x1) — Decrements measurement counter and traverses linked list of data nodes.  ·refined
- `B0059:AB1C` (x2) — Copies 5 bytes from Y to X and U simultaneously.  ·refined
- `B0059:AB2E` (x1) — Checks mode flag M201D for value 2 to select display parameters.  ·refined
- `B0059:AD14` (x2) — Compares D to M28F1, loads full-scale constant if zero.  ·refined
- `B0059:ADAF` (x2) — Selects display buffer end pointer based on M201D mode flag.  ·refined
- `B0059:ADD4` (x2) — Computes display row offset and draws one row of measurement data.  ·refined
- `B0059:AE06` (x3) — Validates pointer in display buffer range, masks byte with stack value.  ·refined
- `B0059:AE1A` (x3) — ORs byte at X with stack value if address is in overlay ROM range.  ·refined
- `B0059:AE2E` (x1) — Sets cursor bit on first row, clears it on subsequent four rows.  ·refined
- `B0059:AE62` (x2) — Stores parameter 5 to stack for display loop operation.  ·refined
- `B0059:AF69` (x1) — Computes absolute delta (M28D7-M28D9) with sign flags for waveform/histogram scaling.  ·refined
- `B0059:AFFA` (x1) — Dispatches measurement display based on sign flag and mode flags M201D, M237E, M2080.  ·refined
- `B0059:B02D` (x1) — Prints unit label (space or 's') for measurement value at M2687.  ·refined
- `B0059:B053` (x1) — Calculates time delta (M28D7-M28D9) and threshold difference (M2F6B-M2F6C) for display.  ·refined
- `B0059:B0A0` (x1) — Selects display string based on sign flag, zero flag, and threshold comparison (M2F6C vs M2F6B).  ·refined
- `B0059:B0A9` (x1) — Continues string selection, prints via Z5A54, then conditionally calls ZA774 based on M2080.  ·refined
- `B0059:B0BA` (x1) — Prints selected string via Z5A54, then tests M2080 for conditional ZA774 call.  ·refined
- `B0059:B156` (x9) — Conditionally displays M268B value or advances cursor 9 columns for measurement field.  ·refined
- `B0059:B16D` (x2) — Conditionally displays M2687 value or advances cursor 8 columns for secondary measurement.  ·refined
- `B0059:B19C` (x3) — Handles status field: calls ZF099 if M268B non-zero, else advances cursor 9 columns.  ·refined
- `B0059:B1B3` (x3) — Display measurement data using 8279 keyboard/display and kernel formatting.  ·refined
- `B0059:B20D` (x1) — Render measurement menu with "Max x to o" based on mode flags.  ·refined
- `B0059:B236` (x1) — Render "Min x to o" menu using histogram/waveform display templates.  ·refined
- `B0059:B243` (x1) — Render "Mean x to o" menu with submenu flag selection.  ·refined
- `B0059:B24F` (x1) — Render "Std. Dev." menu using histogram display templates.  ·refined
- `B0059:B25C` (x1) — Render "Maximum" menu with histogram mode selection.  ·refined
- `B0059:B275` (x1) — Render "Mean" menu using statistics display templates.  ·refined
- `B0059:B28A` (x1) — Render "Maximum" menu with submenu flag selection.  ·refined
- `B0059:B2A8` (x1) — Render "Mean" menu using waveform display templates.  ·refined
- `B0059:B2BD` (x1) — Render "Maximum" menu with histogram display templates.  ·refined
- `B0059:B2DB` (x1) — prints "Mean"
- `B0059:B2F0` (x1) — prints "Maximum"
- `B0059:B30E` (x1) — prints "Minimum"
- `B0059:B31B` (x1) — prints "Mean"
- `B0059:B333` (x1) — prints "Std. Dev."
- `B0059:B36B` (x1) — prints "Mean x to o"
- `B0059:B378` (x1) — prints "Std. Dev."
- `B0059:B3AF` (x1) — prints "Max x to o"
- `B0059:B3C3` (x1) — prints "Min x to o"
- `B0059:B3D6` (x1) — prints "Max x to o"
- `B0059:B3EF` (x2) — Sets highlight bit in waveform display buffer when mode/sub-mode match.  ·refined
- `B0059:B458` (x1) — Tests M26A4 flag to decide clearing measurement data for histogram/waveform init.  ·refined
- `B0059:B4D2` (x1) — Calls ZB75C to format display, then tests M28D6 for end-of-data status.  ·refined
- `B0059:B54D` (x1) — Reloads data pointer from stack for waveform display list entry with $AA/$EA markers.  ·refined
- `B0059:B57E` (x1) — Stores B low byte into display buffer as part of waveform/histogram list entry.  ·refined
- `B0059:B5E7` (x1) — Saves updated U pointer to stack frame $12,S during display buffer construction.  ·refined
- `B0059:B694` (x1) — Calls ZB75C for data conversion in waveform/histogram display routine.  ·refined
- `B0059:B6CF` (x1) — Loads pointer to measurement format table MB448 for display formatting.  ·refined
- `B0059:B6DE` (x1) — Unconditional BRA skips alternative code path in measurement UI string construction.  ·refined
- `B0059:B753` (x2) — Subtracts base pointer U from D for threshold classification binning.  ·refined
- `B0059:B75C` (x9) — Compare sample value to threshold 4 for bin classification.
- `B0059:B769` (x3) — Compare sample value to threshold 4 for bin classification.
- `B0059:B7E2` (x2) — Load pointer to waveform data structure from parameter block.
- `B0059:B88A` (x2) — Transfer second nibble to A for character conversion in display.
- `B0059:B8A2` (x4) — Load next data word from U (counting loop)
- `B0059:B8C3` (x1) — Load a 16-bit value from offset 4 of a structure (X), mask it with constants M2976/M2977, and compare it to a corresponding field in another structure (Y); this is a pattern-match or trigger-compare routine in the measurement UI (B0059).
- `B0059:B8E9` (x3) — Conditional branch selecting between mask and increment constants in histogram cursor logic.
- `B0059:B9BE` (x2) — calls kernel routine ZDFAB to update display list pointer
- `B0059:BAB9` (x1) — prints "Max"
- `B0059:BAD6` (x1) — prints "Min"
- `B0059:BAF2` (x1) — Advances display pointer past a field in measurement UI template.  ·refined
- `B0059:BB08` (x3) — Initializes pointers and counters for measurement UI field advance.  ·refined
- `B0059:BBDC` (x1) — Computes histogram/statistical differences using subtraction loop on M2984.  ·refined
- `B0059:BCB7` (x1) — Computes waveform/histogram display address via LEAU D,X in measurement UI.  ·refined
- `B0059:BD90` (x1) — Calls display configuration routine Z55C7 with argument 3 for measurement UI.  ·refined
- `B0059:BE09` (x1) — Tests lower 3 bits of measurement state to determine waveform/histogram mode.  ·refined
- `B0059:BE58` (x3) — Key input handler checking for 'S' (Save/Store) in measurement menu.  ·refined
- `B0059:BE5F` (x1) — Validates waveform selection against maximum allowed value M29EB.  ·refined
- `B0059:BE6F` (x3) — Parses digit character to select waveform/histogram display format type.  ·refined
- `B0059:BEDE` (x1) — Loads current menu/state pointer from RAM variable M29EB for processing.  ·refined
- `B0059:BF50` (x1) — prints "ERROR"
- `B0059:BF5A` (x1) — prints "RUNNING"
- `B0059:BF64` (x1) — prints "INSERT"
- `B0059:BF6E` (x1) — Load CRTC display base address from I/O register ME025.

## B0060 — MEASUREMENT overlay  (152 functions)

- `B0060:8088` (x8) — Store accumulator to offset 4 in X (likely a structure field)
- `B0060:811B` (x1) — Compare for display control code 0x1C (formatting dispatch).
- `B0060:8229` (x3) — Load display format code $0710 for histogram rendering.
- `B0060:8355` (x2) — This tests a flag at M25D4 to decide whether to clear a range of RAM (M25D7–M25FB) or call a subroutine, part of initialization or state reset in the measurement overlay (B0060).
- `B0060:83F1` (x1) — Clear work RAM variable M2600 (measurement state flag).
- `B0060:8509` (x6) — Load B with 7 as counter for writing pattern to buffer.
- `B0060:8520` (x1) — This CMPX #Z0000 compares X to zero to check for a null pointer, likely validating a linked list or buffer pointer before initializing display data structures in the measurement overlay.
- `B0060:856A` (x1) — Load accumulator with value 5 for HP-IB byte counter or parameter.
- `B0060:8580` (x12) — Load accumulator with value 5 for HP-IB byte counter or parameter.
- `B0060:8604` (x8) — Stores A (value #$01) into M207F, part of a routine in the measurement overlay (B0060) that sets a flag, calls Z8580 (a subroutine), clears the flag, and loops with different A values (1-5) to perform repeated operations, likely for display or measurement updates.
- `B0060:860C` (x1) — Load loop counter for HP-IB command sequence.
- `B0060:8680` (x2) — This instruction ORs A with $30 to convert a 4-bit nibble (0-9) to its ASCII character ('0'-'9'), part of a hex-to-ASCII conversion routine for displaying numeric values in the measurement overlay (ROM B0060).
- `B0060:86F3` (x4) — Loads U from the stack (offset 4) to restore a pointer used in a string/display processing routine in B0060, part of a menu or message formatting function.
- `B0060:8779` (x3) — LBSR to display_string routine at $D089 (banked overlay)
- `B0060:8782` (x1) — LBSR to display_string routine at $D089 (banked overlay)
- `B0060:8803` (x2) — Load X with address of table M8668 (display format data).
- `B0060:881C` (x1) — Branch if first measurement equals threshold to skip error increment.
- `B0060:891C` (x1) — Call subroutine Z8882 (copy table to display buffer)
- `B0060:89A8` (x2) — Call subroutine at Z5661 (unknown, possibly measurement utility).
- `B0060:89E4` (x1) — Call subroutine at Z8C80 (unknown, possibly string/table lookup).
- `B0060:89F7` (x1) — LDX #M2000 loads X with address $2000, starting a loop that processes data from DRAM (work RAM) in the measurement overlay (B0060), likely for waveform or histogram display.
- `B0060:8A2F` (x2) — This loads register B with 7, setting a loop counter for processing 7 bytes (e.g., a 7-byte data field or header), part of a routine that iterates over a buffer and calls a conversion/validation subroutine (Z8CCC) in the measurement overlay (B0060).
- `B0060:8A40` (x3) — This loads register B with 7, setting a loop counter for processing 7 bytes (e.g., a 7-byte data field or header), part of a routine that iterates over a buffer and calls a conversion/validation subroutine (Z8CCC) in the measurement overlay (B0060).
- `B0060:8A4B` (x1) — Call to kernel display routine via RAM vector at $3004.
- `B0060:8A67` (x1) — This instruction loads a byte from a string table at X and increments X, part of a display output routine in ROM B0060 that sends measurement overlay text characters to the CRT via the MC6845 controller.
- `B0060:8AE9` (x1) — Load X with address M0017 for HP-IB buffer or parameter
- `B0060:8AF6` (x1) — Call subroutine Z3008 for acquisition or timing engine setup
- `B0060:8B12` (x1) — Calls subroutine Z8CCC (likely a display or formatting routine) with A loaded from M2002 (a status or mode byte), part of the measurement overlay (B0060) for outputting measurement results (e.g., histogram/waveform data).
- `B0060:8B6D` (x1) — This calls subroutine Z8D04 with parameters loaded from memory (M2F5B and M2F59), part of a routine that formats or processes measurement data for display in the measurement overlay.
- `B0060:8B7F` (x1) — This loads the immediate value 0x0017 into X, likely as a parameter for a subsequent subroutine call (ZD637) that processes a display-list template or menu item in the measurement overlay (B0060).
- `B0060:8B8B` (x1) — This loads the immediate value 0x0017 into X, likely as a parameter for a subsequent subroutine call (ZD637) that processes a display-list template or menu item in the measurement overlay (B0060).
- `B0060:8BA7` (x1) — This calls a subroutine (Z8C80) with the constant $5241 ("RA" in ASCII), part of the measurement overlay (B0060) that likely formats or outputs a status string (e.g., "RAM" or "RANGE") for display or self-test.
- `B0060:8BB3` (x1) — This calls a subroutine (Z8C80) with the constant $5241 ("RA" in ASCII), part of the measurement overlay (B0060) that likely formats or outputs a status string (e.g., "RAM" or "RANGE") for display or self-test.
- `B0060:8BF9` (x1) — Call subroutine to output character to CRT via display_string.
- `B0060:8C3C` (x1) — Loads Y from absolute address $2693 (a pointer or counter), part of the measurement overlay (B0060) that sets up parameters for a waveform or histogram display routine, likely fetching a base address or data pointer.
- `B0060:8C80` (x18) — This instruction calls subroutine ZD5C7 via LBSR, part of the measurement overlay (B0060) for performing a display or data conversion operation, likely related to waveform or histogram rendering.
- `B0060:8CCC` (x22) — Zero the HP-IB byte counter for disc write.
- `B0060:8CD7` (x5) — Zero the HP-IB byte counter for disc write.
- `B0060:8D7E` (x2) — Call subroutine ZD637 (display string or cursor positioning).
- `B0060:8DA1` (x2) — Call subroutine ZD637 (display string or cursor positioning).
- `B0060:8DB7` (x3) — Load X with address of RAM variable M266B (display list template).
- `B0060:8E00` (x1) — Branch back to continue first block copy to trampoline table.
- `B0060:8E29` (x1) — This LDA #$01 / STA MDFF1 sets a flag at $DFF1 to enable the MMU or bank switching, initiating the overlay trampoline setup for measurement overlay B0060.
- `B0060:8EF1` (x4) — LDY M2A25 - This loads a counter value from global variable M2A25 in the measurement overlay (B0060), used as a loop count for a delay or timing routine (Z8EF7) that calls Z922C repeatedly.
- `B0060:901D` (x1) — Compare with byte at $2002 (acquisition parameter or threshold).
- `B0060:9096` (x1) — Clamps measurement result to 1000 and stores in M2695.  ·refined
- `B0060:9186` (x4) — Verifies disc/HP-IB read by comparing returned block address to requested.  ·refined
- `B0060:91B4` (x5) — Initializes loop counter to 6 for processing measurement channels.  ·refined
- `B0060:9280` (x19) — Calls Z922C twice to fetch and combine two measurement bytes.  ·refined
- `B0060:9287` (x9) — Clears M2996 and M2995 flags for state/timing configuration.  ·refined
- `B0060:9291` (x5) — XORs stack byte with M2996 for CRC/checksum on data table M1AAC.  ·refined
- `B0060:92BF` (x1) — Allocates two buffers via ZD5C7 for histogram bin data.  ·refined
- `B0060:938C` (x3) — Polls Z922C in loop until result is zero.  ·refined
- `B0060:9431` (x2) — Sets M29D2 flag if M29C1 equals 1 and key bit 6 is set.  ·refined
- `B0060:944D` (x1) — Calls ZA7C0 measurement/acquisition routine with stack frame.  ·refined
- `B0060:9490` (x3) — prints "LOCAL LOCKOUT"
- `B0060:9646` (x2) — Advance cursor column, wrap to next row if at line end.
- `B0060:965E` (x1) — This instruction calls a subroutine (ZD5C7) that likely performs a conversion or lookup related to measurement data, as part of the measurement overlay (B0060) for histogram/waveform/statistics UI.
- `B0060:974D` (x3) — Branch if menu selection validation failed (carry set).
- `B0060:98C7` (x1) — Store display position for measurement overlay text output.
- `B0060:9982` (x4) — Test flag byte on stack for comma-separated field state.
- `B0060:99A6` (x7) — Load U with address M0100 (base of measurement data buffer).
- `B0060:9A12` (x1) — Compare to '(' for delimiter detection in field parser Z9A12
- `B0060:9A72` (x2) — This RTS returns from a subroutine that searches a table of 4-byte entries (comparing D against values at U) in the measurement overlay ROM (B0060), used for lookup operations.
- `B0060:9A80` (x1) — Calls subroutine ZD1E6 with A=8, part of a data processing routine in B0060 that compares values and handles a specific condition (likely a checksum or format check) before continuing acquisition or display.
- `B0060:9A89` (x1) — Calls subroutine ZD1E6 with A=8, part of a data processing routine in B0060 that compares values and handles a specific condition (likely a checksum or format check) before continuing acquisition or display.
- `B0060:9AC3` (x3) — This compares the B register to $7A (ASCII 'z'), part of a case-conversion routine that converts lowercase letters (a-z) to uppercase by subtracting $20 — a general-purpose string utility in the measurement overlay (ROM B0060).
- `B0060:9AE3` (x1) — Compare U register to address MB7DA, likely a buffer or table limit.
- `B0060:9B3C` (x7) — Compare X to stack limit, check if pointer exceeds buffer end.
- `B0060:9C40` (x1) — This stores the address of a fixed buffer (Z4008) into memory location Z2F54, setting up a pointer for a subroutine that manipulates or saves the processor state (CC register), part of a context-saving or interrupt-like mechanism in the measurement overlay.
- `B0060:9C58` (x4) — Load X with address $4010 (another RAM buffer for measurement).
- `B0060:9C74` (x2) — Load X with address $4014 (third RAM buffer for measurement).
- `B0060:9CB9` (x1) — Store pointer to global variable Z2F54 (display list pointer).
- `B0060:9CBF` (x1) — Read keyboard/display controller status from I/O $E8AA.
- `B0060:9CD8` (x1) — Puls the condition code register (CC) from the stack, restoring the interrupt state after saving it, as part of a wrapper routine that calls Z9BF8 (a subroutine in the measurement overlay B0060) with a modified stack frame.
- `B0060:9CF9` (x1) — Load pointer to bank table entry M4044 for overlay dispatch.
- `B0060:9D2B` (x1) — This PULS restores the X register and program counter, returning from a subroutine that manages overlay bank switching (storing/restoring bank pointers at Z2F54) in the measurement overlay ROM (B0060).
- `B0060:9D44` (x1) — PSHS X,A pushes X and A onto the stack, saving registers for a subroutine call in the measurement overlay (B0060) that manipulates acquisition or display parameters.
- `B0060:9D84` (x1) — Call routine to finalize display or data formatting
- `B0060:9E1E` (x1) — Call ZD5C7 (display string or cursor positioning routine).
- `B0060:9EE3` (x1) — Loads D from M29C2 (LDD M29C2), a limit or threshold value used in a comparison to cap a computed offset for waveform/histogram rendering in the measurement overlay (B0060).
- `B0060:9F9E` (x1) — Calls subroutine Z9FBA to set the FIRQ interrupt mask (SEIF instruction), part of a synchronization or critical section in the measurement overlay (B0060), likely for acquisition timing control.
- `B0060:9FBA` (x1) — Increment counter at M2A29 (measurement sequence step).
- `B0060:A01C` (x1) — conditional branch based on signed comparison of measurement difference (trigger delay).
- `B0060:A026` (x1) — conditional branch based on signed comparison of measurement difference (trigger delay).
- `B0060:A03C` (x1) — Compute horizontal span of measurement bar for display width check.  ·refined
- `B0060:A051` (x1) — Align display pointer to even boundary and decrement character counter.  ·refined
- `B0060:A085` (x2) — Clamp M28F6 threshold value to MBE80 maximum before math conversion.  ·refined
- `B0060:A095` (x1) — Set up display cursor for menu item 7 with offset M0010.  ·refined
- `B0060:A197` (x1) — Test M25D5/M25D6/M2627 flags to conditionally call Z2447 measurement update.  ·refined
- `B0060:A1A4` (x1) — Skip Z2447 call if M25D6 flag is zero in measurement overlay.  ·refined
- `B0060:A281` (x1) — Zero-fill memory block from X to address on stack.  ·refined
- `B0060:A28D` (x2) — Convert overlay address to trampoline base, clamping to $C000 if too high.  ·refined
- `B0060:A2AD` (x1) — Subtract M28EF offset and clamp to zero for MMU page calculation.  ·refined
- `B0060:A2E3` (x3) — Scale value via ZA61E and clamp to 1024 for waveform horizontal coordinate.  ·refined
- `B0060:A301` (x1) — Computes horizontal coordinates for two waveform traces using scaling routine ZA2E6 and stores to M2FBC/M2FBE.  ·refined
- `B0060:A3AC` (x1) — Sets up display structure with parameter 4 via kernel calls ZD5C7/ZD637 and trampoline table MC58D.  ·refined
- `B0060:A3F9` (x1) — Processes display elements in loop using trampoline table and kernel call ZD637 with command callback Z4688.  ·refined
- `B0060:A41E` (x1) — Constructs command jump table entry by storing JMP opcode ($7E) and comparing adjacent bytes in U table.  ·refined
- `B0060:A4F4` (x3) — Clears 8-byte block at X-2 through X-8 by storing zero words (subroutine epilogue).  ·refined
- `B0060:A50F` (x1) — Selects sample count (1000 or 1020) based on measurement mode M201D and calls mode-specific subroutine ZA54D.  ·refined
- `B0060:A54D` (x1) — Processes display list entry from M28D7 using pointer M2645 and string subroutine ZA56C.  ·refined
- `B0060:A56C` (x3) — Performs floating-point arithmetic via Z4E2F/Z65F7/Z69DF using measurement table M2F35 and mode flag M2083.  ·refined
- `B0060:A5B3` (x1) — Copies X to U as pointer for measurement data processing via Z69C4/Z6823 based on M20C4/M20C6.  ·refined
- `B0060:A5FF` (x1) — Selects pointer table (M263A or M263C) based on flag M28D5 for menu item display via ZA61E.  ·refined
- `B0060:A60A` (x1) — Selects pointer/label table pair based on M28D5 flag for measurement UI.  ·refined
- `B0060:A61E` (x2) — Computes measurement parameter difference and formats result via Z65F7/Z4E2F/Z69DF.  ·refined
- `B0060:A6B5` (x4) — Indexes into dispatch table using shifted accumulator, calls Z69DF.  ·refined
- `B0060:A6C3` (x2) — Conditionally sets MMU page registers for measurement channels based on M2083.  ·refined
- `B0060:A6DB` (x3) — Configures hardware for channel groups 4 and 5 via ZD5C7/ZD637.  ·refined
- `B0060:A6E3` (x1) — Configures hardware for channel groups 4 and 5 via ZD5C7/ZD637.  ·refined
- `B0060:A6F4` (x1) — Configures hardware for channel groups 4 and 5 via ZD5C7/ZD637.  ·refined
- `B0060:A6FF` (x3) — Configures hardware for channel groups 4 and 5 via ZD637.  ·refined
- `B0060:A757` (x1) — Stores update flag and copies measurement results to display buffer.  ·refined
- `B0060:A767` (x1) — Copies measurement results M3530-M3536 to display buffer M3514-M351A.  ·refined
- `B0060:A7BC` (x14) — Accumulates 16-bit buffer values into histogram via ZADD8 until counter M3268 zero.  ·refined
- `B0060:A7F7` (x3) — Copies string to display buffer M278F at index M3278, skipping if index >=64.  ·refined
- `B0060:A826` (x5) — Shifts 16-bit value left one bit for histogram data scaling.  ·refined
- `B0060:A833` (x1) — Initializes measurement UI by checking M325A flag and building channel data pointer from M355E.  ·refined
- `B0060:A9E5` (x6) — Converts signed offset to unsigned range by adding $40, storing sign flag for waveform display.  ·refined
- `B0060:AA2D` (x2) — Extracts two 5-bit fields from a 16-bit value for bit-field decoding.  ·refined
- `B0060:AABA` (x1) — Masks command to 0-7 and dispatches bulk operation handler.  ·refined
- `B0060:AB1E` (x1) — Masks B with $C0 to decode HP-IB command high-order bits.  ·refined
- `B0060:AB82` (x27) — Increments HP-IB byte counter and stores 16-bit result to M326C.  ·refined
- `B0060:ABB5` (x1) — Calls ZA9E5 for measurement calculation, then passes result to display routine ZAF6E.  ·refined
- `B0060:ABCD` (x1) — Stores running accumulator results for measurement display pointer/value.  ·refined
- `B0060:AC1B` (x1) — Decodes operation type and saves pointer/value to running accumulators.  ·refined
- `B0060:ACE0` (x1) — Converts raw byte to signed 4-bit amplitude for measurement data.  ·refined
- `B0060:ACF5` (x1) — Validates field index against measurement data table bounds M355C/M355E.  ·refined
- `B0060:ADD8` (x2) — Handles zero byte in display list by calling character handler ZAABA.  ·refined
- `B0060:AE13` (x1) — Checks M325A validity before copying measurement data to waveform buffer M2DB0.  ·refined
- `B0060:AF0A` (x3) — Loads D from M268F for bounds check in display-list rendering.  ·refined
- `B0060:AF6E` (x3) — Clears stack flag and conditionally calls ZAF0A for table evaluation.  ·refined
- `B0060:B3CE` (x2) — Sets loop count and attributes for 4-channel data processing.  ·refined
- `B0060:B47D` (x1) — Loads M26D4 pointer and checks acquisition status at E800/E802.  ·refined
- `B0060:B63A` (x1) — Loads measurement state machine and dispatches histogram calculation if mode 3.  ·refined
- `B0060:B7B3` (x2) — Scans word list for sample crossing lower threshold in pulse edge detection.  ·refined
- `B0060:B7E1` (x2) — Scans word list for sample exceeding upper threshold in pulse edge detection.  ·refined
- `B0060:B80F` (x1) — Binary search of sorted word list for threshold or display parameter match.  ·refined
- `B0060:B837` (x1) — Backward scan of word list for threshold match in display list search.  ·refined
- `B0060:B867` (x1) — Allocates stack locals and loads three display list fields for measurement validity.  ·refined
- `B0060:B8F3` (x1) — Loads display list pointer from stack for waveform or histogram rendering.  ·refined
- `B0060:BC35` (x1) — Calls display, comparison, and arithmetic subroutines for measurement parameter processing.  ·refined
- `B0060:BC83` (x1) — Formats numeric measurement value with sign handling and column offset adjustment.  ·refined
- `B0060:BD4D` (x1) — Sets local flag if instrument mode equals 2 for measurement overlay configuration.  ·refined
- `B0060:BDC0` (x1) — Call subroutine ZBDE5 (likely a display or measurement update).
- `B0060:BDE5` (x1) — This tests the byte at M2003 to check a flag, determining whether to proceed with a measurement UI display update or skip it in ROM B0060.
- `B0060:BE29` (x1) — Increment measurement counter at $2669 (histogram bin).
- `B0060:BED2` (x1) — Masks accumulator A with #$F8 (clearing lower 3 bits) and stores to M2718, part of a configuration routine in the measurement overlay (B0060) that sets up display or acquisition mode bits based on a value in M2578.
- `B0060:BF36` (x1) — Unconditional branch to unit processing after table load
- `B0060:BF99` (x1) — Store zero to 8279 data port $E84E (clear display/keyboard).

## B0061 — KERNEL — reset, MMU, printf, self-test  (161 functions)

- `B0061:C00C` (x1) — Call subroutine ZCB82 (write command to 8279/CRTC)
- `B0061:C051` (x1) — Call subroutine ZBC5C (likely HP-IB or serial byte fetch).
- `B0061:C062` (x2) — Call subroutine ZBC5C (likely HP-IB or serial byte fetch).
- `B0061:C126` (x1) — Call to overlay routine (B0059) for measurement display update.
- `B0061:C161` (x1) — Clear acquisition state flags and test status for HP-IB dispatch.  ·refined
- `B0061:C1F7` (x1) — Write stack byte to ME862 and call keyboard/display interrupt handler.  ·refined
- `B0061:C20A` (x1) — Dispatch command byte to keyboard, display, or acquisition engine handler.  ·refined
- `B0061:C3B5` (x1) — Clear bit 5 of ME863 to enable acquisition trigger.  ·refined
- `B0061:C3C1` (x1) — Call subroutines to clear timing flags and acquisition control bits.  ·refined
- `B0061:C3CD` (x6) — OR accumulator with ME880 and write to ME860 for acquisition control.  ·refined
- `B0061:C3D9` (x3) — AND complement of accumulator with ME880 and write to ME860.  ·refined
- `B0061:C3E1` (x3) — Clear bit 5 of ME860 and reset acquisition state flags M2FF6-M2FF9.  ·refined
- `B0061:C3EF` (x4) — Clear bit 4 of ME860 and reset timing state flag M2FF5.  ·refined
- `B0061:C45D` (x2) — Write HP-IB command $5F and data byte, then wait for transfer completion.  ·refined
- `B0061:C46F` (x3) — Sends HP-IB byte count and secondary address command via I/O registers.  ·refined
- `B0061:C480` (x4) — Polls 8279 status, stores scan code and data into input buffer.  ·refined
- `B0061:C49D` (x2) — Starts acquisition engine if counter >1, else calls abort setup.  ·refined
- `B0061:C563` (x4) — Stores byte to buffer at M29CC, increments pointer and event counter M1841.  ·refined
- `B0061:C57D` (x3) — Resets event counter to 2, stores D into buffer, calls acquisition start.  ·refined
- `B0061:C595` (x1) — Tests flags, calls overlay ZB44D, outputs CR, updates display cursor.  ·refined
- `B0061:C5FB` (x3) — Copies M29CE pointer to M29CC, clears event counter M1841.  ·refined
- `B0061:C60A` (x1) — Searches 4-byte table MB7BA for match against indirect value at M29CE.  ·refined
- `B0061:C639` (x3) — Clears DRAM work RAM from $0000 via ZF230.  ·refined
- `B0061:C644` (x1) — Sets flag M2A69, calls ZB44D, clears M2A28, calls ZC5FB.  ·refined
- `B0061:C653` (x1) — Save condition codes register on stack (interrupt state preservation).
- `B0061:C6C0` (x1) — Call subroutine to process keyboard/display via Intel 8279
- `B0061:C6E3` (x3) — Load A from I/O register $E883 (acquisition/threshold subsystem)
- `B0061:C6F4` (x3) — Call subroutine ZC710 for initialization using cleared B counter.
- `B0061:C710` (x3) — This stores accumulator A into the memory location pointed to by X (from $29C5) and increments X — part of a character output buffer routine in the kernel (B0061) that writes to a circular buffer for the display/keyboard subsystem (Intel 8279 at $E848-$E84F).
- `B0061:C733` (x1) — Push U, X, D registers for buffer flush or display update
- `B0061:C8E8` (x1) — This BLE instruction checks if A ≤ 1, controlling a loop that processes keyboard or display data in the kernel's initialization or self-test routine (B0061).
- `B0061:C940` (x1) — This LDA loads the constant $04 to set a loop count for copying 4 bytes from U to X, part of a kernel routine in B0061 that copies data between buffers (likely for I/O or display).
- `B0061:C94D` (x2) — LDX loads a pointer from the stack to copy a 10-byte block (likely a display line or configuration data) from one buffer to another in a string or menu template copy operation.
- `B0061:C974` (x12) — Loads B with 4 to initialize a loop counter for a BCD-to-binary conversion routine in the kernel (B0061), converting packed BCD digits from a buffer into a binary value.
- `B0061:C97D` (x5) — BCD-to-binary conversion loop for packed digits in kernel.  ·refined
- `B0061:CA2F` (x1) — Displays configuration header string via display_string with B=1.  ·refined
- `B0061:CA38` (x1) — Displays configuration string at M254C via display_string with B=1.  ·refined
- `B0061:CA7E` (x2) — Table lookup loop comparing B against entries at X.  ·refined
- `B0061:CA94` (x1) — Multi-way dispatch for error code 0 message display.  ·refined
- `B0061:CA9F` (x1) — Multi-way dispatch for error code 0 second message display.  ·refined
- `B0061:CAA8` (x1) — Multi-way dispatch for error code 2 second message display.  ·refined
- `B0061:CAB7` (x1) — Self-test error code switch comparing B to 3.  ·refined
- `B0061:CAC0` (x1) — Branch to common exit after case 3 error messages.  ·refined
- `B0061:CACA` (x1) — Branch to common exit after case 3 second message display.  ·refined
- `B0061:CAD3` (x1) — Branch to common exit after displaying case 3 messages.
- `B0061:CAE3` (x1) — Call display routine to print first case 4/6 message.
- `B0061:CAEC` (x1) — Call display routine to print first case 4/6 message.
- `B0061:CB82` (x23) — Call Z5FF8 (likely routine in bank $05 for analog)
- `B0061:CC31` (x1) — Call subroutine to read Intel 8279 keyboard/display status.
- `B0061:CCBA` (x2) — This computes the address of a stack-based buffer (offset $05 from S) for a subroutine call, likely formatting or parsing data from the display list templates.
- `B0061:CDD7` (x1) — Return from BULK routine (configuration display).
- `B0061:CE05` (x1) — Call subroutine at $7076 (likely string/display routine)
- `B0061:CE21` (x1) — Load space character for display formatting
- `B0061:CEB7` (x1) — Clears a byte on the stack (CLR $02,S) to initialize a temporary variable, part of a bit-shifting or data extraction routine in the resident kernel (B0061), likely for decoding trigger or timing information.
- `B0061:CEC5` (x1) — Accumulates config table bits into stack byte, branching on counter match.  ·refined
- `B0061:CFAD` (x1) — Stores display list pointer and attribute into menu structure fields.  ·refined
- `B0061:D01C` (x1) — Saves D, then calls two subroutines for menu line processing.  ·refined
- `B0061:D028` (x2) — Initializes menu display by loading selection byte and building line buffer.  ·refined
- `B0061:D2B6` (x1) — Sets up menu field pointers and calls display initialization subroutine.  ·refined
- `B0061:D34A` (x2) — Conditionally doubles summed data bytes based on flag M266A for scaling.  ·refined
- `B0061:D382` (x3) — Multiplies waveform samples by scaling factors from M2FAB/M2FAD tables.  ·refined
- `B0061:D3DB` (x1) — Outputs characters to CRTC until cursor reaches limit M28E3.  ·refined
- `B0061:D3F2` (x1) — Moves cursor home/down/right via ZD412 for CRT display driver.  ·refined
- `B0061:D412` (x4) — Processes CRTC cursor movement with font multiplication for pixel output.  ·refined
- `B0061:D4A2` (x1) — Compares A to 1, part of a routine that selects a divisor or scaling factor (e.g., $28, $14, $0A) based on a mode variable (M28D6), likely for timebase or sample rate configuration.
- `B0061:D4D6` (x1) — Compare accumulator B with immediate value #$02 (check for a specific mode or state).
- `B0061:D8EF` (x1) — Save current display mode flag to stack for later restore
- `B0061:D908` (x1) — This return-from-subroutine exits a function that temporarily saves and restores a mode flag (M28D6) around a call to ZD3C3, which likely controls a display or acquisition state in the kernel.
- `B0061:D924` (x1) — Return from subroutine that sets display mode to 5 (no refresh call)
- `B0061:D93D` (x1) — Call subroutine ZDB9B (likely display update/refresh)
- `B0061:D959` (x1) — This calls subroutine ZD3C3 with A=#01, which is the kernel's bank-switching or MMU page-setup routine, used here to temporarily map a specific overlay bank before calling ZDBC1 (likely a disc or HP-IB I/O operation).
- `B0061:DA0A` (x1) — This shifts register B left by one bit (ASLB), part of a loop that doubles a 16-bit value and stores it, likely a data scaling or address calculation routine in the kernel (e.g., for memory mapping or buffer management).
- `B0061:DA2B` (x1) — Arithmetic left shift of accumulator A (ASLA) as part of a loop that doubles 16-bit values from a source buffer (X) and stores them to a destination buffer (U), likely scaling data for waveform or histogram display.
- `B0061:DA5E` (x1) — This adjusts the stack pointer down by 6 bytes (LEAS -$06,S) to allocate local variables, part of a kernel routine (B0061) that processes or copies data (likely related to MMU page setup or buffer management, as indicated by the subsequent TFR and LEAY operations).
- `B0061:DB0C` (x1) — Dispatch routine selecting timing/scale constants for parameter values 2,4,5.  ·refined
- `B0061:DBEF` (x1) — Initializes stack frame from configuration templates at RAM $2F31 and $2F35.  ·refined
- `B0061:DC2C` (x1) — Initializes timer channel $0C and clears counters at RAM $2F6D.  ·refined
- `B0061:DDC9` (x4) — Selects character table pointer ($5116 or $51B6) based on flags at $C57D/$266A.  ·refined
- `B0061:DF68` (x1) — Saves display position to $2DAE and outputs carriage return via string at $DF64.  ·refined
- `B0061:E0D1` (x1) — Sets stack flag from rear-panel switch bit3 and checks M29BF for self-test mode.  ·refined
- `B0061:E23B` (x2) — Processes keyboard buffer at M29C5, checking pointer against buffer end M19A9.  ·refined
- `B0061:E240` (x1) — Loads keyboard read pointer from M29C5 and compares to buffer end M19A9.  ·refined
- `B0061:E247` (x1) — Loads key byte from buffer, compares pointer to end M19A9, writes to $E8A7.  ·refined
- `B0061:E32B` (x1) — Writes $10 and $8F commands to Intel 8279 keyboard/display controller.  ·refined
- `B0061:E38B` (x1) — Loads PCR-relative pointer to HP-IB error message table for display.  ·refined
- `B0061:E643` (x1) — Initializes a chain of 10-byte HP-IB parameter buffers in kernel RAM.  ·refined
- `B0061:E681` (x1) — Loads PCR-relative pointer to string table ME416 for kernel utility call.  ·refined
- `B0061:E7BF` (x10) — Copies a NUL-terminated display list template from U to buffer at X.  ·refined
- `B0061:E7D2` (x3) — Allocates stack frame and checks M2001 status flag for acquisition hardware.  ·refined
- `B0061:E885` (x1) — Converts two stack parameters to decimal and stores at M1FBA/M1FBC.  ·refined
- `B0061:E8B8` (x1) — Branches to display routine after loading string pointer for trigger wait.  ·refined
- `B0061:E8CA` (x1) — Loads string pointer for "Waiting for Sequence Term #" and formats digit.  ·refined
- `B0061:E8DC` (x1) — Loads string pointer for "Waiting for Stop Term" and branches to display.  ·refined
- `B0061:E8E7` (x1) — Stores ORed control byte to M2993 and calls I/O routine with command $35.  ·refined
- `B0061:E9AB` (x1) — prints "Waiting for Timing Trigger"
- `B0061:E9C7` (x1) — prints "Delaying"
- `B0061:E9D2` (x1) — prints "Timing Trace in Process"
- `B0061:E9E3` (x1) — prints "Timing:     Secs to Completion"
- `B0061:EABC` (x1) — prints "Waiting for Analog Trigger"
- `B0061:EAC8` (x1) — prints "Delaying"
- `B0061:EAD3` (x1) — prints "Analog Trace in Process"
- `B0061:EAE4` (x1) — prints "Analog:     Secs to Completion"
- `B0061:EB36` (x1) — LBSR    Z9230                    * Position CRTC cursor for menu text.
- `B0061:EC99` (x6) — Branch if carry clear to decrement counter in display attribute loop.
- `B0061:ECC4` (x1) — Test byte at $270B (RAM flag, possibly display mode or state).
- `B0061:EE2F` (x2) — Compare channel index B against maximum channel count (3).
- `B0061:EE6D` (x1) — Increment saturation counter for second channel.
- `B0061:EE90` (x1) — Branch if equal (tests a stack flag), part of a routine that selects either 0x0F or 0x20 as a byte to store to a CRTC display memory location (U points to a screen buffer), controlling display attributes (e.g., inverse video or blanking) in the kernel (B0061).
- `B0061:EEA4` (x1) — Set B=3 for special display attribute (e.g., cursor/underline).
- `B0061:EF40` (x1) — Stores the low byte of a 16-bit value to the Intel 8279 keyboard/display controller data port (ME84E), sending a command or data byte to initialize or update the front-panel display.
- `B0061:EF64` (x1) — Load value 3 to set 8279 display mode (e.g., left entry, 8-digit).
- `B0061:EF76` (x1) — This instruction checks if A is not equal to 3, testing a configuration or mode flag (M2080), part of a kernel routine (B0061) that controls the self-test or initialization sequence (e.g., deciding whether to run a delay loop or skip to another test).
- `B0061:F076` (x1) — SWI interrupt handler; jumps to reset vector for system restart
- `B0061:F089` (x3) — main_entry — MAIN routine (X=$FAD7 cmd/cfg ptr, D=$4000). Boot flows here
- `B0061:F147` (x3) — Disable interrupts at entry of display cursor update routine.
- `B0061:F17C` (x2) — Load X from address ZE039, a pointer to current display or state structure.
- `B0061:F1C1` (x2) — Branch if timeout counter non-zero in timer interrupt handler.
- `B0061:F1CB` (x1) — Branch if timeout counter non-zero in timer interrupt handler.
- `B0061:F1E6` (x9) — Increment counter/index in configuration loop.
- `B0061:F230` (x8) — Call kernel subroutine ZFCAC (interrupt handler)
- `B0061:F291` (x1) — Push all registers onto stack (interrupt handler entry)
- `B0061:F2A7` (x1) — This pushes registers (PSHS U,Y,X,DP,D,CC) to save context at the start of an interrupt service routine or kernel trap handler, as indicated by the SEIF (set entire interrupt flag) and stack manipulation.
- `B0061:F2B9` (x1) — This pushes registers (PSHS U,Y,X,DP,D,CC) to save context at the start of an interrupt service routine or kernel trap handler, as indicated by the SEIF (set entire interrupt flag) and stack manipulation.
- `B0061:F31F` (x1) — init_sub_F31F — LBSR'd from init_hw
- `B0061:F4C4` (x1) — prints "Error in ROM #"
- `B0061:F4D2` (x1) — prints "ROM ok"
- `B0061:F4D8` (x1) — prints "Error in RAM"
- `B0061:F4E1` (x1) — prints "expected"
- `B0061:F50D` (x1) — Call display_hex_byte to print computed checksum.
- `B0061:F53F` (x2) — RTS at F54E returns from a subroutine that converts a nibble to ASCII hex and stores it to a display buffer, part of the kernel's hex-to-display utility (called by display_string or similar).
- `B0061:F54F` (x5) — display_string — **the common "printf"/puts**: X=$00-terminated string
- `B0061:F585` (x1) — Set CRTC control bit0 for cursor enable in video routine.
- `B0061:F5A7` (x1) — Write modified value to keyboard/display register $E848
- `B0061:F5C7` (x16) — Set X to character pattern pointer from mapping table (font).
- `B0061:F637` (x35) — mmu_set_page — (A=logical page idx, D/X=phys) -> writes $FF00 page regs
- `B0061:F664` (x1) — clear_mem — CLR ,X+ ; SUBD #1 ; BNE  (block clear)
- `B0061:F6C3` (x1) — pre_reset_sub — BSR'd before reboot
- `B0061:F74F` (x1) — Return from CRTC register set (0,4) subroutine.
- `B0061:F754` (x1) — Return from CRTC register set (0,4) subroutine.
- `B0061:F75A` (x1) — Save condition codes for CRTC sequence routine.
- `B0061:F823` (x6) — Branches if Y (loaded from a linked-list node) is non-zero to skip storing Y at U+2, part of a kernel linked-list traversal routine (likely for managing overlay or buffer chains) at F823.
- `B0061:F83B` (x3) — Checks if a linked-list node pointer (X) equals a saved value on the stack, part of a kernel linked-list traversal routine (B0061) for managing display or menu structures.
- `B0061:F898` (x6) — Stores accumulator A into I/O register $E844 (likely a data latch for the acquisition engine), part of a kernel routine (ROM B0061) that writes a byte to the capture FIFO or sample clock control, with B stored at $E845.
- `B0061:F8A2` (x2) — Loads pointer from I/O register $E029 (current acquisition buffer offset).
- `B0061:F8B3` (x3) — Loads pointer from I/O register $E029 (current acquisition buffer offset).
- `B0061:F8E6` (x2) — Store U to CRTC register ME02B (cursor end or attribute).
- `B0061:F8F2` (x4) — Store U to CRTC register ME02B (cursor end or attribute).
- `B0061:F93B` (x2) — Compare B (end column) to max column 63 for CRTC bounds.
- `B0061:F982` (x4) — Branch if carry clear to skip decrement of ME02B (display line pointer).
- `B0061:F992` (x1) — Call kernel subroutine at F8F2 (likely display sync or cursor set).
- `B0061:FA54` (x2) — LDB $04,S - Load signed byte from stack (e.g., cursor offset parameter).
- `B0061:FAA5` (x7) — This BLT instruction compares the accumulator A against a value at I/O register ME02E (0xE02E), checking if a parameter (likely a cursor or trigger position) is within bounds, part of a boundary validation routine in the resident kernel (B0061).
- `B0061:FBAB` (x1) — STY stores Y (set to $FFFF) into the MC6845 CRTC register at offset $32 from base ZE039, configuring the cursor or display start address during FIRQ interrupt handling.
- `B0061:FCAC` (x4) — Load B with #$06 and multiply, part of a kernel routine (B0061) that validates a keyboard scan code (DECA, compare to $20) and then indexes into a key mapping table at ME043, likely for translating scan codes to character or function codes.
- `B0061:FCE8` (x3) — This ASLB instruction shifts a value left (multiply by 2) to index into a table at ME103, used for decoding or mapping a key code or status value in the keyboard/display handler.
- `B0061:FCFC` (x2) — This RTS returns from a character attribute encoding subroutine in the kernel (B0061) that converts character codes to CRT control bytes for the display system, setting bit 4 of B when bit 5 of A is set.
- `B0061:FD80` (x1) — RESET — LDY #0; LBSR init_hw; RTS
- `B0061:FDC8` (x2) — init_hw — ORCC #$50 (mask I+F); copy MMU table; init CRTC/8279; LDS #$E1FD
- `B0061:FE58` (x1) — Advance display position pointer U by 4 (CRTC cursor step)
- `B0061:FEAA` (x1) — Load X with MFAD7 (pointer to self-test message table)
- `B0061:FEB9` (x7) — init_sub_FEB9 — LBSR'd 3x from init_hw (per-entry MMU/struct setup)
